# Конфигурация Cursor для работы с Claude Code Bridge

## 📋 Настройка Cursor

### 1. Добавление Bridge MCP Server в Cursor

**Файл**: `.cursor/mcp.json` (в корне проекта) или `~/.config/cursor/mcp.json` (глобально)

```json
{
  "mcpServers": {
    "docker-mcp-orchestrator": {
      "command": "python",
      "args": ["-m", "orchestrator"],
      "cwd": "/absolute/path/to/docker-mcp-orchestrator",
      "env": {
        "PYTHONPATH": "/absolute/path/to/docker-mcp-orchestrator/src"
      }
    },
    "cursor-claude-code-bridge": {
      "command": "python",
      "args": ["-m", "bridge"],
      "cwd": "/absolute/path/to/cursor-claude-code-bridge",
      "env": {
        "PYTHONPATH": "/absolute/path/to/cursor-claude-code-bridge/src",
        "ANTHROPIC_API_KEY": "your-anthropic-api-key-here",
        "BRIDGE_CONFIG_PATH": "/absolute/path/to/cursor-claude-code-bridge/config/config.yaml"
      }
    }
  }
}
```

**Примечания**:
- Используйте абсолютные пути для `cwd`
- API ключ можно также хранить в переменных окружения системы
- После изменения конфигурации перезапустите Cursor

---

### 2. Настройка Cursor Rules

**Файл**: `.cursorrules` (в корне проекта)

```markdown
# Claude Code Integration Rules

## Работа с Claude Code через Bridge

При получении запроса от пользователя для сложных задач:

1. **Улучшение запроса**:
   - Уточни все неясные моменты у пользователя
   - Определи все требования и ограничения
   - Добавь контекст проекта (релевантные файлы, зависимости)
   - Форматируй запрос в структурированный формат

2. **Отправка запроса**:
   - Используй tool `enhance_and_send_request` для отправки улучшенного запроса
   - Сохрани `task_id` для отслеживания прогресса
   - Информируй пользователя о начале выполнения

3. **Отслеживание прогресса**:
   - Периодически проверяй статус через `get_task_status`
   - Показывай прогресс пользователю
   - Обрабатывай ошибки и информируй пользователя

4. **Получение результатов**:
   - Используй `get_task_results` для получения финальных результатов
   - Представь результаты в понятном формате
   - Предложи дальнейшие действия

## Формат улучшенного запроса

При подготовке запроса для Claude Code используй следующий формат:

```json
{
  "task": "четкое описание основной задачи",
  "requirements": [
    "требование 1",
    "требование 2",
    "требование 3"
  ],
  "context": {
    "files": ["список релевантных файлов"],
    "dependencies": ["список зависимостей"],
    "project_structure": "описание структуры проекта"
  },
  "expected_output": "описание ожидаемого результата",
  "constraints": [
    "ограничение 1",
    "ограничение 2"
  ],
  "priority": "medium",
  "deadline": null
}
```

## Примеры использования

### Пример 1: Простая задача
```
Пользователь: "Создай функцию валидации email"

Действия:
1. Уточни требования (какой язык? нужны ли тесты?)
2. Подготовь структурированный запрос
3. Отправь через enhance_and_send_request
4. Отследи выполнение
5. Покажи результаты
```

### Пример 2: Сложная задача
```
Пользователь: "Рефакторинг модуля аутентификации"

Действия:
1. Уточни детали (что именно рефакторить? какие требования?)
2. Изучи текущий код модуля
3. Подготовь детальный запрос с контекстом
4. Отправь через enhance_and_send_request
5. Регулярно проверяй статус
6. Покажи результаты по мере выполнения
```

## Важные правила

- ✅ Всегда улучшай запрос перед отправкой
- ✅ Всегда используй Bridge tools для взаимодействия с Claude Code
- ✅ Всегда отслеживай прогресс выполнения
- ✅ Всегда информируй пользователя о статусе
- ❌ НЕ отправляй необработанные запросы пользователя напрямую
- ❌ НЕ игнорируй ошибки выполнения
- ❌ НЕ забывай проверять статус задач
```

---

### 3. Промпты для улучшения запросов

**Файл**: `prompts/cursor_request_enhancement.md`

```markdown
# Request Enhancement Prompt

Ты - помощник для улучшения запросов пользователей перед отправкой в Claude Code оркестратор.

## Твоя задача

При получении запроса от пользователя:

1. **Анализ запроса**:
   - Определи, является ли запрос достаточно детальным
   - Выяви неясные моменты
   - Определи, какая информация отсутствует

2. **Уточнение** (если необходимо):
   - Задай уточняющие вопросы пользователю
   - Получи недостающую информацию
   - Убедись, что все требования понятны

3. **Форматирование**:
   - Преобразуй запрос в структурированный JSON формат
   - Добавь контекст проекта (если доступен)
   - Определи приоритет и ограничения

4. **Улучшение**:
   - Используй четкие и конкретные формулировки
   - Добавь технические детали где необходимо
   - Убедись, что запрос понятен для AI оркестратора

## Формат выходных данных

После обработки запроса верни JSON в следующем формате:

```json
{
  "task": "четкое описание задачи",
  "requirements": ["требование 1", "требование 2"],
  "context": {
    "files": ["file1.py", "file2.py"],
    "dependencies": ["dependency1", "dependency2"],
    "project_structure": "описание структуры"
  },
  "expected_output": "описание ожидаемого результата",
  "constraints": ["ограничение 1", "ограничение 2"],
  "priority": "low" | "medium" | "high",
  "deadline": null | "ISO datetime"
}
```

## Примеры улучшения

### Пример 1: Неясный запрос

**Исходный запрос**: "Сделай что-то с API"

**Улучшенный запрос**:
```json
{
  "task": "Создать REST API endpoint для управления пользователями",
  "requirements": [
    "Endpoint должен поддерживать CRUD операции",
    "Должна быть реализована валидация входных данных",
    "Должна быть добавлена обработка ошибок"
  ],
  "context": {
    "files": ["src/api/users.py", "src/models/user.py"],
    "dependencies": ["fastapi", "pydantic", "sqlalchemy"]
  },
  "expected_output": "Рабочий API endpoint с тестами и документацией",
  "constraints": [
    "Использовать существующую структуру проекта",
    "Следовать стилю кода проекта"
  ],
  "priority": "medium"
}
```

### Пример 2: Уже детальный запрос

**Исходный запрос**: "Создай функцию для валидации email адресов на Python с использованием регулярных выражений. Функция должна принимать строку и возвращать boolean. Нужны тесты и docstring."

**Улучшенный запрос**:
```json
{
  "task": "Создать функцию для валидации email адресов на Python",
  "requirements": [
    "Использовать регулярные выражения",
    "Функция должна принимать строку (email: str)",
    "Функция должна возвращать boolean",
    "Добавить docstring с описанием и примерами",
    "Написать unit тесты для функции",
    "Обработать edge cases (пустая строка, None, невалидные форматы)"
  ],
  "context": {
    "files": ["src/utils/validation.py"],
    "dependencies": ["re (стандартная библиотека)"]
  },
  "expected_output": "Функция validate_email(email: str) -> bool с тестами и документацией",
  "constraints": [
    "Использовать только стандартную библиотеку Python",
    "Следовать PEP 8",
    "Покрытие тестами не менее 90%"
  ],
  "priority": "medium"
}
```

## Правила улучшения

1. **Четкость**: Используй конкретные формулировки вместо общих
2. **Детализация**: Добавь технические детали где необходимо
3. **Контекст**: Включи релевантный контекст проекта
4. **Требования**: Разбей сложные требования на простые пункты
5. **Ограничения**: Укажи все важные ограничения
6. **Приоритет**: Определи приоритет на основе важности задачи
```

---

### 4. Пример использования в Cursor

**Сценарий**: Пользователь хочет создать REST API

**Диалог**:

```
Пользователь: "Создай REST API для блога"

Cursor AI: 
Я помогу тебе создать REST API для блога. Чтобы подготовить детальный запрос для Claude Code, мне нужно уточнить несколько моментов:

1. Какой фреймворк использовать? (FastAPI, Flask, Django REST Framework?)
2. Какие endpoints нужны? (создание поста, получение постов, обновление, удаление, комментарии?)
3. Какая база данных? (PostgreSQL, SQLite, MongoDB?)
4. Нужна ли аутентификация?
5. Нужны ли тесты?

[Пользователь отвечает на вопросы]

Cursor AI:
Отлично! Теперь подготовлю детальный запрос и отправлю его в Claude Code для выполнения.

[Использует tool: enhance_and_send_request]

Cursor AI:
Запрос отправлен! Task ID: abc123-def456-ghi789

Claude Code проанализировал задачу и создал план выполнения:
- Task 1: Настройка проекта и зависимостей (Code Agent)
- Task 2: Создание моделей данных (Code Agent)
- Task 3: Создание API endpoints (Code Agent)
- Task 4: Написание тестов (Test Agent)
- Task 5: Обновление документации (Docs Agent)

Ожидаемое время выполнения: ~15 минут

Отслеживаю прогресс...

[Периодически проверяет статус через get_task_status]

Cursor AI:
Прогресс выполнения:
✅ Task 1: Завершено
✅ Task 2: Завершено
🔄 Task 3: В процессе
⏳ Task 4: Ожидает
⏳ Task 5: Ожидает

[После завершения использует get_task_results]

Cursor AI:
Все задачи выполнены! Результаты:

✅ Создан проект с FastAPI
✅ Настроены модели данных (Post, Comment, User)
✅ Созданы API endpoints:
   - POST /api/posts
   - GET /api/posts
   - GET /api/posts/{id}
   - PUT /api/posts/{id}
   - DELETE /api/posts/{id}
✅ Написаны тесты (покрытие: 95%)
✅ Обновлена документация

Файлы созданы:
- src/api/posts.py
- src/models/post.py
- tests/test_posts.py
- README.md

Хочешь, чтобы я показал код какого-то конкретного файла?
```

---

### 5. Переменные окружения

**Файл**: `.env` (в корне проекта, добавьте в `.gitignore`)

```bash
# Anthropic API Key для Claude Code
ANTHROPIC_API_KEY=sk-ant-api03-...

# Bridge Server Configuration
BRIDGE_CONFIG_PATH=/path/to/cursor-claude-code-bridge/config/config.yaml
BRIDGE_LOG_LEVEL=INFO

# Orchestrator Configuration
ORCHESTRATOR_CONFIG_PATH=/path/to/docker-mcp-orchestrator/config/config.yaml

# Optional: Custom Claude Model
CLAUDE_MODEL=claude-3-5-sonnet-20241022
CLAUDE_MAX_TOKENS=4096
CLAUDE_TEMPERATURE=0.7
```

---

### 6. Проверка настройки

**Скрипт для проверки**: `scripts/check_setup.sh`

```bash
#!/bin/bash

echo "Проверка настройки Cursor-Claude Code Bridge..."

# Проверка наличия MCP конфигурации
if [ -f "$HOME/.config/cursor/mcp.json" ]; then
    echo "✅ MCP конфигурация найдена"
else
    echo "❌ MCP конфигурация не найдена: $HOME/.config/cursor/mcp.json"
fi

# Проверка API ключа
if [ -z "$ANTHROPIC_API_KEY" ]; then
    echo "⚠️  ANTHROPIC_API_KEY не установлен"
else
    echo "✅ ANTHROPIC_API_KEY установлен"
fi

# Проверка Python зависимостей
if python -c "import anthropic" 2>/dev/null; then
    echo "✅ Anthropic SDK установлен"
else
    echo "❌ Anthropic SDK не установлен. Установите: pip install anthropic"
fi

# Проверка Bridge Server
if [ -d "/path/to/cursor-claude-code-bridge" ]; then
    echo "✅ Bridge Server найден"
else
    echo "❌ Bridge Server не найден. Проверьте путь в mcp.json"
fi

echo "Проверка завершена!"
```

---

Это полная конфигурация для работы Cursor с Claude Code Bridge. После настройки Cursor сможет автоматически улучшать запросы и отправлять их в Claude Code для оркестрации выполнения через субагентов.
