# Резюме архитектуры Docker MCP Orchestrator V2

## 🎯 Ключевые изменения от V1

### 1. Orchestrator как Proxy (Критическое изменение)

**V1**: Orchestrator управлял видимостью tools через enable/disable, но AI взаимодействовал с серверами напрямую через Docker MCP Toolkit Gateway.

**V2**: Orchestrator работает как **полноценный proxy** - все взаимодействие AI с запущенными MCP серверами идет через Orchestrator.

```
AI → Orchestrator.call_tool() → MCP Server → Результат → AI
```

**Преимущества**:
- ✅ Не требуется перезапуск соединения Cursor
- ✅ Полный контроль над взаимодействием
- ✅ Единая точка входа для всех серверов
- ✅ Возможность логирования и мониторинга

---

### 2. Расширенный набор Tools

**V1**: 6 tools для управления серверами.

**V2**: ~14 tools, разделенных на группы:

#### Группа 1: Управление серверами (5 tools)
- `list_installed_servers` - список установленных серверов
- `list_catalog_servers` - список серверов в каталоге (для установки)
- `start_servers` - запуск серверов
- `stop_servers` - остановка серверов
- `get_active_servers` - список активных серверов

#### Группа 2: Информация (2 tools)
- `get_server_tools` - метаданные о tools сервера
- `get_server_info` - детальная информация о сервере

#### Группа 3: Управление конфигурацией (5 tools)
- `config_set` - установка конфигурации
- `config_get` - получение конфигурации
- `secret_set` - установка секрета
- `secret_list` - список секретов
- `secret_remove` - удаление секрета

#### Группа 4: Proxy (2 tools)
- `call_tool` - вызов tool через proxy
- `list_active_tools` - список активных tools

---

### 3. Orchestrator НЕ анализирует

**V1**: Архитектура предполагала анализ задач для определения нужных серверов.

**V2**: Orchestrator - это **инструмент для AI**. AI сам решает:
- Какие серверы нужны для задачи
- Когда запускать/останавливать серверы
- Какие tools использовать

Orchestrator только предоставляет инструменты для управления.

---

### 4. Управление промптами

**Новое в V2**: При запуске сервера Orchestrator:
1. Извлекает промпт сервера (если есть)
2. Возвращает промпт AI вместе с информацией о tools
3. AI добавляет промпт в контекст для правильной работы с сервером

**Важно**: Промпты необходимы для правильной работы многих MCP серверов.

---

## 🔄 Как это работает

### Workflow взаимодействия AI с Orchestrator

```
1. AI: list_installed_servers()
   → Получает список установленных MCP серверов

2. AI: get_server_tools("github-mcp")
   → Получает метаданные о tools без запуска сервера

3. AI: config_set("github-mcp", {"GITHUB_TOKEN": "..."})
   → Настраивает конфигурацию перед запуском

4. AI: start_servers(["github-mcp"])
   → Orchestrator:
      - Активирует сервер через Docker MCP Toolkit
      - Устанавливает MCP соединение с сервером
      - Регистрирует tools в Proxy
      - Извлекает промпт сервера
   → Возвращает AI:
      - Список активных tools
      - Промпт для добавления в контекст
      - Статус запуска

5. AI: Добавляет промпт в контекст

6. AI: call_tool("github_create_repo", {"name": "my-repo"})
   → Orchestrator:
      - Определяет сервер по имени tool (github-mcp)
      - Маршрутизирует запрос к серверу
      - Выполняет tool через MCP Protocol
   → Возвращает результат AI

7. AI: stop_servers(["github-mcp"])
   → Orchestrator:
      - Закрывает соединение с сервером
      - Деактивирует сервер через Docker MCP Toolkit
```

---

## 🏗️ Архитектурная схема

```
┌─────────────┐
│   Cursor    │
│  (AI Client)│
└──────┬──────┘
       │ MCP Protocol (stdio)
       │ Единственное соединение
       ▼
┌─────────────────────────────────────┐
│  Orchestrator MCP Server            │
│                                     │
│  Tools (~14):                      │
│  - Управление серверами (5)        │
│  - Информация (2)                  │
│  - Конфигурация (5)                │
│  - Proxy (2)                       │
│                                     │
│  Proxy Layer:                      │
│  ┌─────────────────────────────┐   │
│  │ Connection Pool            │   │
│  │ - github-mcp (MCP conn)    │   │
│  │ - postgres-mcp (MCP conn)  │   │
│  │ - ...                      │   │
│  └─────────────────────────────┘   │
│                                     │
│  Tool Router:                      │
│  - tool_name → server_name         │
└──────┬──────────────────────────────┘
       │
       ├─► Docker MCP Toolkit CLI
       │   (docker mcp server enable/disable)
       │
       └─► MCP Connections
           ├─► github-mcp
           ├─► postgres-mcp
           └─► ... (другие запущенные серверы)
```

---

## ✅ Соответствие требованиям

| Требование | Статус | Решение |
|------------|--------|---------|
| Расширенный набор tools | ✅ | ~14 tools для управления и proxy |
| Управление конфигурацией | ✅ | config_set, secret_set и др. |
| Proxy для взаимодействия | ✅ | call_tool через Orchestrator |
| Управление промптами | ✅ | Автоматическое извлечение и предоставление |
| Orchestrator как инструмент | ✅ | AI сам принимает решения |

---

## ✅ Соответствие ограничениям

| Ограничение | Статус | Решение |
|-------------|--------|---------|
| Cursor 2.x требует перезапуск | ✅ | Proxy - не требуется перезапуск |
| MCP Protocol ограничения | ✅ | Orchestrator устанавливает соединения |
| Docker MCP Toolkit CLI | ✅ | Используется для управления |
| Token Budget | ✅ | Серверы запускаются по требованию |

---

## 🎯 Ключевые компоненты

### 1. Proxy Layer
- Маршрутизация запросов к серверам
- Управление соединениями
- Обработка ошибок и переподключение

### 2. Connection Pool
- Пул активных соединений с MCP серверами
- Автоматическое переподключение
- Управление жизненным циклом соединений

### 3. Prompt Manager
- Извлечение промптов из метаданных
- Предоставление промптов AI при запуске

### 4. Docker Client
- Интеграция с Docker MCP Toolkit
- Управление серверами и конфигурацией

---

## 🔍 Критические вопросы для исследования

### 1. Механизм подключения к запущенным серверам

**Вопрос**: Как Orchestrator подключается к серверам, запущенным через Docker MCP Toolkit?

**Варианты**:
- Через Docker MCP Toolkit gateway API
- Прямое подключение к контейнерам
- Использование SSE/streaming transport

**Требуется**: Изучить документацию Docker MCP Toolkit.

### 2. Хранение промптов

**Вопрос**: Где хранятся промпты для MCP серверов?

**Варианты**:
- В метаданных Catalog
- В конфигурации сервера
- В отдельном файле/ресурсе

**Требуется**: Изучить структуру Catalog.

---

## 📊 Преимущества архитектуры V2

1. **Полный контроль**: Все взаимодействие идет через Orchestrator
2. **Гибкость**: AI сам решает, какие серверы использовать
3. **Расширяемость**: Легко добавлять новые tools
4. **Надежность**: Централизованная обработка ошибок
5. **Прозрачность**: Логирование всех операций

---

## 📚 Документы

- **ARCHITECTURE_V2.md** - детальное описание архитектуры
- **IMPLEMENTATION_PLAN_V2.md** - план реализации
- **ARCHITECTURE_SUMMARY_V2.md** - это резюме

---

## 🚀 Следующие шаги

1. ✅ Архитектура переработана с учетом требований
2. ⏳ **Исследование механизма подключения к серверам**
3. ⏳ **Создание прототипа Proxy**
4. ⏳ Начало реализации согласно плану

---

**Готов к исследованию и реализации после утверждения архитектуры!** 🚀
