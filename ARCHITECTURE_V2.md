# Архитектура Docker MCP Orchestrator V2

## 📐 Обзор архитектуры

Docker MCP Orchestrator V2 — это инструмент для AI, который предоставляет управление MCP серверами и работает как **proxy/gateway** для взаимодействия AI с запущенными MCP серверами. Orchestrator НЕ анализирует и не принимает решения — это инструмент, которым управляет AI.

---

## 🏗️ Компоненты системы

### 1. Orchestrator MCP Server (Основной компонент)

**Назначение**: MCP сервер, который подключается к Cursor и предоставляет tools для управления MCP серверами и **proxy** для взаимодействия с запущенными серверами.

**Ключевая функция**: Orchestrator работает как **единая точка входа** для всех взаимодействий AI с MCP серверами.

**Tools, предоставляемые Orchestrator**:

#### Группа 1: Управление серверами
1. `list_installed_servers` - получить список установленных MCP серверов (из Docker MCP Catalog)
2. `list_catalog_servers` - получить список всех доступных серверов в каталоге (для установки новых)
3. `start_servers` - запустить указанные MCP серверы через Docker MCP Toolkit
4. `stop_servers` - остановить указанные MCP серверы
5. `get_active_servers` - получить список активных (запущенных) серверов

#### Группа 2: Информация о серверах
6. `get_server_tools` - получить список tools от указанного сервера (метаданные)
7. `get_server_info` - получить детальную информацию о сервере (описание, требования, промпты)

#### Группа 3: Управление конфигурацией
8. `config_set` - установить конфигурацию для MCP сервера (через `docker mcp config`)
9. `config_get` - получить конфигурацию MCP сервера
10. `secret_set` - установить секрет для MCP сервера (через `docker mcp secret`)
11. `secret_list` - получить список секретов
12. `secret_remove` - удалить секрет

#### Группа 4: Proxy для взаимодействия с запущенными серверами
13. `call_tool` - вызвать tool от запущенного MCP сервера через Orchestrator (proxy)
14. `list_active_tools` - получить список всех доступных tools от активных серверов

**Важно**: Orchestrator предоставляет **~14 tools** для управления и **proxy функциональность** для взаимодействия с запущенными серверами.

---

### 2. Proxy Layer (Критический компонент)

**Назначение**: Проксирование всех команд AI к запущенным MCP серверам через Orchestrator.

**Проблема**: После запуска MCP сервера через Docker MCP Toolkit, AI должен иметь возможность взаимодействовать с их tools. Но Cursor требует перезапуска соединения для обнаружения новых tools.

**Решение**: Orchestrator работает как **proxy/gateway**, который:
1. Запускает MCP серверы через Docker MCP Toolkit
2. Поддерживает соединения с запущенными серверами
3. Проксирует вызовы tools от AI к соответствующим серверам
4. Возвращает результаты обратно AI

**Архитектура Proxy**:
```
AI → Orchestrator.call_tool("github_create_repo", {...})
    │
    ├─► Определение сервера по имени tool
    │
    ├─► Маршрутизация к соответствующему MCP серверу
    │
    ├─► Выполнение tool через MCP Protocol
    │
    └─► Возврат результата AI
```

**Реализация**:
- Orchestrator поддерживает пул соединений с запущенными MCP серверами
- При запуске сервера через `start_servers`, Orchestrator устанавливает MCP соединение с этим сервером
- Все вызовы tools проходят через Orchestrator
- Orchestrator маршрутизирует запросы к нужному серверу на основе имени tool

---

### 3. Docker MCP Toolkit Integration Layer

**Назначение**: Интеграция с Docker MCP Toolkit для управления серверами и конфигурацией.

**Функции**:
- Выполнение CLI команд `docker mcp`
- Управление жизненным циклом серверов (enable/disable через gateway)
- Управление конфигурацией и секретами
- Получение информации о серверах

**Используемые команды**:
- `docker mcp catalog show [name] --format=json` - список серверов в каталоге
- `docker mcp server ls --json` - список активных серверов
- `docker mcp server enable <server1> <server2> ...` - активация через gateway
- `docker mcp server disable <server1> <server2> ...` - деактивация
- `docker mcp tools ls --format=json` - список tools от активных серверов
- `docker mcp server inspect <server>` - детальная информация
- `docker mcp config read/write` - управление конфигурацией
- `docker mcp secret set/list/rm` - управление секретами

---

### 4. Server Registry & Metadata Cache

**Назначение**: Хранилище метаданных о серверах, их tools и промптах.

**Функции**:
- Кэширование списка установленных серверов
- Хранение метаданных о tools каждого сервера
- Хранение промптов для серверов (если есть)
- Индексация tools по серверам

**Структура данных**:
```json
{
  "servers": {
    "github-mcp": {
      "name": "github-mcp",
      "description": "GitHub integration",
      "installed": true,
      "active": false,
      "tools": [
        {
          "name": "github_create_repo",
          "description": "Create a new repository",
          "inputSchema": {...}
        }
      ],
      "prompt": "# GitHub MCP Server\n\nUse this server for GitHub operations...",
      "config": {
        "GITHUB_TOKEN": "required"
      }
    }
  }
}
```

---

### 5. Prompt Manager

**Назначение**: Управление промптами для MCP серверов.

**Функции**:
- Извлечение промптов из метаданных серверов
- Добавление промптов в контекст AI при запуске сервера
- Хранение промптов в кэше

**Workflow**:
1. При запуске сервера через `start_servers`
2. Orchestrator получает промпт сервера (если есть)
3. Промпт возвращается AI вместе с информацией о tools
4. AI добавляет промпт в свой контекст для правильной работы с сервером

---

## 🔄 Архитектурные паттерны и решения

### Паттерн 1: Proxy Gateway Pattern (Ключевой)

**Проблема**: После запуска MCP сервера через Docker MCP Toolkit, AI должен иметь возможность использовать их tools, но Cursor требует перезапуска соединения для обнаружения новых tools.

**Решение**: Orchestrator работает как **proxy/gateway**:
- Orchestrator запускает серверы через Docker MCP Toolkit
- Orchestrator устанавливает MCP соединения с запущенными серверами
- AI вызывает tools через Orchestrator (`call_tool`)
- Orchestrator маршрутизирует запросы к нужным серверам
- Результаты возвращаются AI через Orchestrator

**Преимущества**:
- ✅ Не требуется перезапуск соединения Cursor
- ✅ Полный контроль над взаимодействием
- ✅ Возможность логирования и мониторинга
- ✅ Единая точка входа для всех MCP серверов

**Недостатки**:
- ⚠️ Дополнительный слой (небольшая задержка)
- ⚠️ Orchestrator должен поддерживать пул соединений

---

### Паттерн 2: Tool-Based Architecture

**Проблема**: AI должен иметь простой и понятный интерфейс для управления MCP серверами.

**Решение**: Все операции выполняются через tools:
- Управление серверами → `start_servers`, `stop_servers`
- Получение информации → `list_installed_servers`, `get_server_tools`
- Управление конфигурацией → `config_set`, `secret_set`
- Взаимодействие с серверами → `call_tool`

**Преимущества**:
- ✅ Единообразный интерфейс
- ✅ Легко расширяемый
- ✅ Понятный для AI

---

### Паттерн 3: Metadata-First with Proxy

**Проблема**: AI нужно знать, какие tools доступны, но активация всех серверов перегружает контекст.

**Решение**:
- AI получает метаданные о tools через `get_server_tools` без активации
- AI выбирает нужные серверы и запускает их через `start_servers`
- После запуска AI получает список активных tools через `list_active_tools`
- AI вызывает tools через `call_tool` (proxy)

**Workflow**:
```
1. AI: list_installed_servers() → получает список установленных серверов
2. AI: get_server_tools("github-mcp") → получает метаданные о tools
3. AI: start_servers(["github-mcp"]) → запускает сервер
4. Orchestrator: запускает сервер, устанавливает соединение, возвращает промпт (если есть)
5. AI: list_active_tools() → получает список доступных tools
6. AI: call_tool("github_create_repo", {...}) → вызывает tool через proxy
7. Orchestrator: маршрутизирует к github-mcp, выполняет, возвращает результат
```

---

## 🔌 Интеграция с Cursor и Docker MCP Toolkit

### Схема подключения

```
┌─────────────┐
│   Cursor    │
│  (AI Client)│
└──────┬──────┘
       │ MCP Protocol (stdio)
       │ Единственное соединение
       ▼
┌─────────────────────────────────────┐
│  Orchestrator MCP Server            │
│  (docker-mcp-orchestrator)          │
│                                     │
│  Tools для управления:              │
│  - list_installed_servers           │
│  - start_servers                   │
│  - stop_servers                    │
│  - get_server_tools                │
│  - config_set/secret_set           │
│  - call_tool (PROXY)               │
│  - list_active_tools               │
│                                     │
│  Proxy Layer:                      │
│  - Пул соединений с MCP серверами   │
│  - Маршрутизация запросов          │
│  - Выполнение tools                │
└──────┬──────────────────────────────┘
       │
       ├─► Docker MCP Toolkit CLI
       │   (docker mcp server enable/disable)
       │
       └─► MCP Connections Pool
           ├─► github-mcp (MCP Protocol)
           ├─► postgres-mcp (MCP Protocol)
           └─► ... (другие запущенные серверы)
```

### Важное замечание о взаимодействии

**Ключевой момент**: После запуска сервера через `start_servers`:
1. Orchestrator активирует сервер через Docker MCP Toolkit
2. Orchestrator устанавливает MCP соединение с сервером
3. AI получает информацию о доступных tools и промпт (если есть)
4. AI вызывает tools через `call_tool` (не напрямую!)
5. Orchestrator проксирует запрос к соответствующему серверу
6. Результат возвращается AI через Orchestrator

**Это означает**: AI НЕ имеет прямого доступа к tools запущенных серверов. Все взаимодействие идет через Orchestrator.

---

## 📊 Потоки данных

### Поток 1: Получение списка установленных серверов

```
AI Request: list_installed_servers()
    │
    ▼
Orchestrator.list_installed_servers()
    │
    ├─► Проверка кэша
    │   │
    │   └─► docker mcp catalog show --format=json
    │       │
    │       └─► Фильтрация установленных серверов
    │           │
    │           └─► Возврат AI
```

### Поток 2: Запуск серверов

```
AI Request: start_servers(["github-mcp", "postgres-mcp"])
    │
    ▼
Orchestrator.start_servers()
    │
    ├─► Валидация списка серверов
    │
    ├─► Активация через Docker MCP Toolkit
    │   │
    │   └─► docker mcp server enable github-mcp postgres-mcp
    │
    ├─► Установка MCP соединений
    │   │
    │   ├─► Создание соединения с github-mcp
    │   └─► Создание соединения с postgres-mcp
    │
    ├─► Получение списка tools от серверов
    │   │
    │   └─► tools/list для каждого сервера
    │
    ├─► Получение промптов (если есть)
    │   │
    │   └─► Из метаданных серверов
    │
    └─► Возврат AI:
        - Список активных tools
        - Промпты для добавления в контекст
        - Статус запуска
```

### Поток 3: Вызов tool через proxy

```
AI Request: call_tool("github_create_repo", {"name": "my-repo"})
    │
    ▼
Orchestrator.call_tool()
    │
    ├─► Определение сервера по имени tool
    │   │
    │   └─► Поиск в реестре активных серверов
    │
    ├─► Проверка доступности соединения
    │   │
    │   └─► Если соединение потеряно → переподключение
    │
    ├─► Маршрутизация к нужному серверу
    │   │
    │   └─► MCP Protocol: tools/call
    │       │
    │       └─► github-mcp выполняет tool
    │
    └─► Возврат результата AI
```

### Поток 4: Управление конфигурацией

```
AI Request: config_set("postgres-mcp", {"DATABASE_URL": "postgres://..."})
    │
    ▼
Orchestrator.config_set()
    │
    ├─► Валидация конфигурации
    │
    ├─► Запись конфигурации
    │   │
    │   └─► docker mcp config write
    │
    └─► Возврат статуса AI
```

---

## 🤖 AI Prompt Rules для работы с Orchestrator

### Правила для AI

```markdown
## Docker MCP Orchestrator Workflow

### Инициализация работы с MCP серверами

1. **Получи список установленных серверов**:
   - Используй `list_installed_servers` для получения списка доступных MCP серверов
   - Изучи описания серверов

2. **Изучи возможности серверов**:
   - Используй `get_server_tools(server_name)` для получения списка tools от сервера
   - Используй `get_server_info(server_name)` для детальной информации
   - Определи, какие серверы нужны для задачи

3. **Настрой конфигурацию (если необходимо)**:
   - Если серверу нужны секреты или конфигурация, используй:
     - `secret_set(key, value)` для установки секретов
     - `config_set(server, config)` для установки конфигурации
   - Пример: для postgres-mcp может потребоваться DATABASE_URL

4. **Запусти нужные серверы**:
   - Используй `start_servers([server1, server2, ...])` для запуска
   - После запуска получи промпт сервера (если есть) и добавь его в контекст
   - Получи список активных tools через `list_active_tools()`

5. **Используй tools через Orchestrator**:
   - ВСЕ вызовы tools должны идти через `call_tool(tool_name, arguments)`
   - НЕ пытайся вызывать tools напрямую - они доступны только через Orchestrator
   - Пример: `call_tool("github_create_repo", {"name": "my-repo"})`

6. **После завершения задачи**:
   - Используй `stop_servers([server1, server2, ...])` для остановки
   - Это освобождает ресурсы и минимизирует контекст

### Важные замечания

- Orchestrator НЕ анализирует задачи - ты сам решаешь, какие серверы нужны
- Все взаимодействие с запущенными серверами идет через Orchestrator (proxy)
- При запуске сервера обязательно получи и используй промпт (если есть)
- Управляй конфигурацией и секретами перед запуском серверов
```

---

## 🔒 Обработка ошибок и надежность

### Стратегии обработки ошибок

1. **Ошибки запуска сервера**:
   - Retry с экспоненциальной задержкой (3 попытки)
   - Логирование ошибки
   - Возврат детальной информации об ошибке AI

2. **Потеря соединения с MCP сервером**:
   - Автоматическое переподключение
   - Проверка состояния соединения перед каждым вызовом
   - Graceful degradation - возврат ошибки AI при недоступности

3. **Ошибки выполнения tool**:
   - Передача ошибки от сервера к AI
   - Логирование для диагностики
   - Retry для временных ошибок (опционально)

4. **Недоступность Docker MCP Toolkit**:
   - Проверка доступности перед выполнением команд
   - Информативное сообщение об ошибке
   - Fallback на кэшированные метаданные

---

## 🚀 Производительность

### Оптимизации

1. **Пул соединений**:
   - Поддержание активных соединений с запущенными серверами
   - Переиспользование соединений
   - Автоматическое закрытие неиспользуемых соединений

2. **Кэширование метаданных**:
   - Кэш списка серверов: TTL 5 минут
   - Кэш метаданных tools: TTL 10 минут
   - Кэш промптов: постоянный

3. **Асинхронная обработка**:
   - Асинхронные вызовы tools
   - Параллельное выполнение независимых операций
   - Неблокирующие операции

4. **Минимизация задержек**:
   - Предварительное установление соединений
   - Кэширование маршрутизации tools → серверы
   - Оптимизация протокола MCP

---

## 📝 Конфигурация

### Структура конфигурации

```yaml
orchestrator:
  # Настройки кэширования
  cache:
    servers_ttl: 300  # 5 минут
    tools_ttl: 600    # 10 минут
  
  # Настройки Docker MCP Toolkit
  docker_mcp:
    catalog: "docker-mcp"  # Имя каталога по умолчанию
    
  # Настройки Proxy
  proxy:
    connection_pool_size: 10  # Максимум одновременных соединений
    connection_timeout: 30   # Таймаут соединения (секунды)
    reconnect_attempts: 3    # Попытки переподключения
    
  # Настройки производительности
  performance:
    server_start_timeout: 10  # секунд
    tool_call_timeout: 30     # секунд
    max_concurrent_tools: 5   # Максимум параллельных вызовов
    
  # Настройки надежности
  reliability:
    retry_attempts: 3
    retry_delay: 1  # секунд (экспоненциальная задержка)
```

---

## ✅ Соответствие требованиям

### BR-1: Zero-Configuration
✅ Пользователь один раз настраивает список серверов в Docker MCP Catalog
✅ Orchestrator предоставляет инструменты для управления

### BR-2: Оптимизация контекста
✅ Серверы запускаются только по требованию AI
✅ AI получает только нужные tools через proxy
✅ Промпты добавляются только для активных серверов

### BR-3: Прозрачность
✅ AI может получить список активных серверов
✅ Логирование всех операций
✅ Метаданные о tools доступны

### BR-4: Масштабируемость
✅ Работа с любым количеством серверов
✅ Пул соединений для эффективности
✅ Кэширование для производительности

### BR-5: Надежность
✅ Обработка ошибок и retry
✅ Автоматическое переподключение
✅ Graceful degradation

### BR-6: Производительность
✅ Кэширование метаданных
✅ Пул соединений
✅ Асинхронная обработка

---

## 🔍 Проверка соответствия ограничениям

### TC-1: Ограничение Cursor 2.x
✅ РЕШЕНО: Orchestrator работает как proxy, не требуется перезапуск соединения Cursor

### TC-2: MCP Protocol ограничения
✅ РЕШЕНО: Orchestrator устанавливает соединения с серверами и проксирует запросы

### TC-3: Docker MCP Toolkit CLI
✅ РЕШЕНО: Используется для управления серверами, JSON формат, retry-логика

### TC-4: Token Budget
✅ РЕШЕНО: Серверы запускаются по требованию, tools доступны через proxy

---

## 🎯 Ключевые отличия от V1

1. **Orchestrator как proxy**: Все взаимодействие с запущенными серверами идет через Orchestrator
2. **Расширенный набор tools**: Добавлены tools для управления конфигурацией и секретами
3. **Управление промптами**: Автоматическое извлечение и предоставление промптов AI
4. **Нет анализа**: Orchestrator не анализирует задачи - это инструмент для AI
5. **Пул соединений**: Orchestrator поддерживает соединения с запущенными серверами

---

## 📚 Следующие шаги

1. ✅ Архитектура переработана с учетом требований
2. ⏳ Реализация Orchestrator MCP Server с proxy функциональностью
3. ⏳ Реализация пула соединений с MCP серверами
4. ⏳ Интеграция с Docker MCP Toolkit
5. ⏳ Реализация управления конфигурацией и секретами
6. ⏳ Реализация управления промптами
7. ⏳ Создание AI Prompt Rules
8. ⏳ Тестирование и оптимизация
