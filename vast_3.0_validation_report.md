# Отчет о валидации VAST 3.0 документа

## ❌ КРИТИЧЕСКИЕ ОШИБКИ (нарушают спецификацию)

### 1. Неверная версия VAST

**Проблема:**
```xml
<VAST ... version="2.0" ...>
```

**Требование VAST 3.0:**
Должно быть `version="3.0"` для соответствия спецификации 3.0.

**Критичность:** 🔴 КРИТИЧЕСКАЯ - документ не соответствует заявленной версии

---

### 2. Пустой элемент `<ClickThrough>`

**Проблема:**
```xml
<VideoClicks>
    <ClickThrough></ClickThrough>
</VideoClicks>
```

**Согласно спецификации VAST 3.0:**
- Элемент `<ClickThrough>` должен содержать валидный URL, если присутствует
- Пустой элемент `<ClickThrough></ClickThrough>` **запрещен**
- Если клик не требуется, элемент должен быть **полностью удален**

**Критичность:** 🔴 КРИТИЧЕСКАЯ - нарушает структуру XML

---

### 3. Отсутствует обязательный элемент `<AdSystem version>`

**Проблема:**
```xml
<AdSystem>Rollover</AdSystem>
```

**Требование VAST 3.0:**
В VAST 3.0 атрибут `version` у элемента `<AdSystem>` является **ОБЯЗАТЕЛЬНЫМ** (required).

**Должно быть:**
```xml
<AdSystem version="1.0">Rollover</AdSystem>
```

**Критичность:** 🔴 КРИТИЧЕСКАЯ - отсутствует обязательный атрибут

---

### 4. Неверный формат атрибута `skipoffset`

**Проблема:**
```xml
<Linear skipoffset="00:00:15">
```

**Требование VAST 3.0:**
В VAST 3.0 атрибут `skipoffset` может быть:
- В формате времени: `HH:MM:SS` или `HH:MM:SS.mmm` (с миллисекундами)
- В формате процента: `[0-9]+%` (например, `25%`)

**Проверка формата:**
- `00:00:15` - формат корректен, но в VAST 3.0 рекомендуется использовать более точный формат с миллисекундами: `00:00:15.000`
- Однако, если длительность ролика 15 секунд, а skipoffset тоже 15 секунд, пропуск будет недоступен

**Критичность:** 🟡 ПРЕДУПРЕЖДЕНИЕ - формат корректен, но не оптимален

---

### 5. Отсутствует обязательный элемент `<Pricing>` (если применимо)

**Требование VAST 3.0:**
Если используется система ценообразования, элемент `<Pricing>` должен присутствовать в `<InLine>`.

**Статус:** ✅ Не требуется, если ценообразование не используется

---

### 6. Отсутствует обязательный элемент `<Survey>` (если применимо)

**Требование VAST 3.0:**
Если используется опрос, элемент `<Survey>` должен присутствовать в `<InLine>`.

**Статус:** ✅ Не требуется, если опрос не используется

---

## ⚠️ СТРУКТУРНЫЕ ПРОБЛЕМЫ

### 7. Отсутствует элемент `<AdServingId>` (рекомендуется)

**Требование VAST 3.0:**
Элемент `<AdServingId>` опционален, но рекомендуется для идентификации сервера показа рекламы.

**Статус:** 🟡 РЕКОМЕНДАЦИЯ

---

### 8. Отсутствует элемент `<Category>` (если применимо)

**Требование VAST 3.0:**
Элемент `<Category>` опционален, но рекомендуется для категоризации рекламы.

**Статус:** 🟡 РЕКОМЕНДАЦИЯ

---

### 9. Проверка элементов `<MediaFile>`

**Текущие атрибуты:**
```xml
<MediaFile 
    id="..." 
    delivery="progressive" 
    type="video/mp4" 
    bitrate="1200" 
    width="854" 
    height="480" 
    isScalable="true" 
    keepAspectRatio="true">
```

**Требования VAST 3.0:**
- ✅ `delivery` - обязательный, присутствует
- ✅ `type` - обязательный, присутствует
- ✅ `width` - обязательный, присутствует
- ✅ `height` - обязательный, присутствует
- ✅ `bitrate` - опциональный, присутствует
- ✅ `isScalable` - опциональный, присутствует (должен быть строкой "true" или "false")
- ✅ `keepAspectRatio` - опциональный, присутствует (должен быть строкой "true" или "false")
- ✅ `minBitrate` - опциональный, отсутствует (не требуется)
- ✅ `maxBitrate` - опциональный, отсутствует (не требуется)
- ✅ `apiFramework` - опциональный, отсутствует (не требуется)

**Статус:** ✅ Все обязательные атрибуты присутствуют, опциональные корректны

---

### 10. Проверка событий Tracking

**События в документе:**
- `creativeView` ✅
- `start` ✅
- `firstQuartile` ✅
- `midpoint` ✅
- `thirdQuartile` ✅
- `complete` ✅

**Дополнительные события VAST 3.0 (отсутствуют, но опциональны):**
- `mute` - опционально
- `unmute` - опционально
- `pause` - опционально
- `resume` - опционально
- `rewind` - опционально
- `fullscreen` - опционально
- `exitFullscreen` - опционально
- `expand` - опционально
- `collapse` - опционально
- `acceptInvitation` - опционально
- `close` - опционально
- `skip` - опционально
- `progress` - опционально

**Статус:** ✅ Все присутствующие события корректны, отсутствующие опциональны

---

### 11. Проверка элемента `<VideoClicks>`

**Текущее:**
```xml
<VideoClicks>
    <ClickThrough></ClickThrough>
</VideoClicks>
```

**Требования VAST 3.0:**
- `<ClickThrough>` - опциональный, но если присутствует, должен содержать URL
- `<ClickTracking>` - опциональный, может быть множественным
- `<CustomClick>` - опциональный, может быть множественным

**Проблема:** Пустой `<ClickThrough>` нарушает спецификацию

**Критичность:** 🔴 КРИТИЧЕСКАЯ

---

### 12. Проверка элемента `<Duration>`

**Текущее:**
```xml
<Duration>00:00:15</Duration>
```

**Требование VAST 3.0:**
Формат должен быть: `HH:MM:SS.mmm` (с миллисекундами) или `HH:MM:SS`

**Статус:** ✅ Формат `00:00:15` корректен, но рекомендуется `00:00:15.000` для полного соответствия

---

### 13. Проверка элемента `<Extensions>`

**Текущее:**
```xml
<Extensions>
    <Extension type="startTime">00:00</Extension>
    <Extension type="skipTime">00:15</Extension>
    <Extension type="skipTime2">00:15</Extension>
    <Extension type="linkTxt">перейти на сайт рекламодателя</Extension>
    <Extension type="isClickable">0</Extension>
    <Extension type="AdTitle">...</Extension>
    <Extension type="AdTune">...</Extension>
</Extensions>
```

**Требование VAST 3.0:**
- Элемент `<Extensions>` опционален
- Элементы `<Extension>` могут быть кастомными
- Тип указывается через атрибут `type`
- Содержимое может быть любым валидным XML

**Статус:** ✅ Структура корректна, но:
- Дублирование `skipTime` и `skipTime2` может указывать на избыточность
- JSON внутри Extension допустим, но лучше использовать валидный XML

---

## 📋 Проверка обязательных элементов VAST 3.0

| Элемент | Требование | Статус | Примечание |
|---------|------------|--------|------------|
| `<VAST version="3.0">` | Обязательно | ❌ | Указано `version="2.0"` |
| `<Ad>` | Обязательно | ✅ | Корректно |
| `<InLine>` | Обязательно | ✅ | Корректно |
| `<AdSystem version="">` | **Обязательно** | ❌ | **Отсутствует атрибут version** |
| `<AdTitle>` | Обязательно | ✅ | Корректно |
| `<Description>` | Опционально | ✅ | Присутствует |
| `<Error>` | Опционально | ✅ | Присутствует |
| `<Impression>` | Обязательно (минимум 1) | ✅ | Присутствует |
| `<Creatives>` | Обязательно | ✅ | Корректно |
| `<Creative>` | Обязательно | ✅ | Корректно |
| `<Linear>` | Обязательно | ✅ | Корректно |
| `<Duration>` | Обязательно | ✅ | Корректно |
| `<TrackingEvents>` | Опционально | ✅ | Присутствует |
| `<VideoClicks>` | Опционально | ⚠️ | Присутствует, но `<ClickThrough>` пустой |
| `<MediaFiles>` | Обязательно | ✅ | Корректно |
| `<MediaFile>` | Обязательно (минимум 1) | ✅ | Присутствует |
| `<Extensions>` | Опционально | ✅ | Присутствует |

---

## 🎯 ИТОГОВЫЙ СПИСОК ПРОБЛЕМ

### 🔴 КРИТИЧЕСКИЕ ОШИБКИ (требуют обязательного исправления):

1. **Неверная версия VAST**
   - Текущее: `version="2.0"`
   - Должно быть: `version="3.0"`

2. **Отсутствует обязательный атрибут `version` у `<AdSystem>`**
   - Текущее: `<AdSystem>Rollover</AdSystem>`
   - Должно быть: `<AdSystem version="1.0">Rollover</AdSystem>`

3. **Пустой элемент `<ClickThrough>`**
   - Текущее: `<ClickThrough></ClickThrough>`
   - Должно быть: Удалить элемент или добавить валидный URL

### 🟡 РЕКОМЕНДАЦИИ (желательно исправить):

4. **Формат `Duration`**
   - Текущее: `00:00:15`
   - Рекомендуется: `00:00:15.000` (с миллисекундами)

5. **Формат `skipoffset`**
   - Текущее: `00:00:15`
   - Рекомендуется: `00:00:15.000` или использовать процент

6. **Логика `skipoffset`**
   - Если длительность = skipoffset, пропуск недоступен
   - Рассмотреть использование процента (например, `25%`)

7. **Дублирование Extension**
   - `skipTime` и `skipTime2` с одинаковыми значениями

---

## ✅ ПОЛОЖИТЕЛЬНЫЕ МОМЕНТЫ

1. ✅ Использование CDATA для всех URL - отличное решение
2. ✅ Правильная структура документа
3. ✅ Все основные обязательные элементы присутствуют (кроме version в AdSystem)
4. ✅ Корректные форматы данных
5. ✅ Множественные MediaFile для разных битрейтов
6. ✅ Корректные события Tracking
7. ✅ Правильные атрибуты MediaFile

---

## 📝 ВЫВОД

Документ **НЕ СООТВЕТСТВУЕТ** спецификации VAST 3.0 из-за **трех критических ошибок**:

1. Неверная версия (`version="2.0"` вместо `version="3.0"`)
2. Отсутствует обязательный атрибут `version` у `<AdSystem>`
3. Пустой элемент `<ClickThrough>`

После исправления этих трех проблем документ будет соответствовать спецификации VAST 3.0.

---

## 🔧 РЕКОМЕНДУЕМЫЕ ИСПРАВЛЕНИЯ

### Исправление 1: Версия VAST
```xml
<!-- Было -->
<VAST ... version="2.0" ...>

<!-- Должно быть -->
<VAST ... version="3.0" ...>
```

### Исправление 2: AdSystem с version
```xml
<!-- Было -->
<AdSystem>Rollover</AdSystem>

<!-- Должно быть -->
<AdSystem version="1.0">Rollover</AdSystem>
```

### Исправление 3: ClickThrough
```xml
<!-- Вариант 1: Удалить, если клик не нужен -->
<VideoClicks>
</VideoClicks>

<!-- Или полностью удалить VideoClicks, если не используется -->

<!-- Вариант 2: Добавить URL, если клик нужен -->
<VideoClicks>
    <ClickThrough><![CDATA[https://example.com]]></ClickThrough>
</VideoClicks>
```

### Исправление 4 (рекомендация): Duration с миллисекундами
```xml
<!-- Было -->
<Duration>00:00:15</Duration>

<!-- Рекомендуется -->
<Duration>00:00:15.000</Duration>
```
