# Docker MCP Orchestrator

## 🎯 Проблема (Pain Point)

### Текущая ситуация

При работе с AI-помощниками (Cursor, Claude Desktop и др.) пользователи сталкиваются с критической проблемой **перегрузки контекста**:

- **Невозможно эффективно использовать множество MCP серверов одновременно**: При подключении всех необходимых MCP серверов к AI-помощнику, в контекст попадают **все tools от всех серверов**, что приводит к:
  - Превышению лимитов токенов контекста
  - Снижению качества ответов AI из-за информационного шума
  - Медленной работе из-за обработки нерелевантных инструментов
  - Необходимости постоянно вручную включать/выключать серверы для разных задач

- **Ручное управление серверами не масштабируется**: 
  - Пользователь должен помнить, какие серверы нужны для каждого типа задач
  - Постоянное переключение между конфигурациями отнимает время
  - Высокий риск ошибок при ручном управлении
  - Невозможность эффективно работать с большим количеством серверов (10+)

### Пример проблемы

**Сценарий**: Разработчик работает над проектом, где ему нужны:
- GitHub MCP для работы с репозиториями
- PostgreSQL MCP для работы с БД
- File System MCP для работы с файлами
- Browser MCP для тестирования

**Проблема**: При подключении всех 4 серверов, AI получает ~200+ tools в контекст, из которых реально используются только 5-10 для конкретной задачи. Это приводит к:
- Замедлению обработки запросов
- Ухудшению качества ответов
- Превышению лимитов токенов

---

## 💡 Решение

**Docker MCP Orchestrator** — система автоматического управления MCP серверами, которая:

1. **Автоматически определяет оптимальный набор серверов** для каждой задачи (ЭТО ГЛАВНАЯ АРХИТЕКТУРНАЯ ЗАДАЧА! Как при запуске определить список нужных MCP серверов, чтобы Docker MCP Orchestrator поднял их и вернул список их tools!?)
2. **Динамически активирует/деактивирует серверы** через Docker MCP Toolkit
3. **Минимизирует контекст AI** — в каждый момент времени активны только релевантные серверы
4. **Работает прозрачно** — пользователь один раз настраивает систему и забывает о ручном управлении

### Ключевые преимущества

- ✅ **Zero-Configuration**: После первоначальной настройки система работает автоматически
- ✅ **Оптимизация контекста**: AI всегда работает с минимальным, но достаточным набором tools
- ✅ **Масштабируемость**: Поддержка неограниченного количества MCP серверов из Docker MCP Catalog
- ✅ **Прозрачность**: Пользователь видит, какие серверы активны и почему
- ✅ **Надежность**: Стабильная работа даже при сбоях отдельных серверов


## 📋 Бизнес-требования

### BR-1: Zero-Configuration для пользователя
**Описание**: После первоначальной установки и настройки пользователь не должен управлять серверами вручную.

**Критерии приемки**:
- Пользователь один раз добавляет MCP проект `docker-mcp-orchestrator` в свой AI-клиент
- Пользователь настраивает список доступных MCP серверов (добавляя сервисы в MCP_DOCKER из Catalog)
- Система автоматически управляет серверами без вмешательства пользователя

**Стратегии оптимизации**:
1. **Минимизация списка tools**: Orchestrator предоставляет минимальный набор tools для определения необходимых MCP серверов
2. **Динамический выбор**: На основе анализа запроса пользователя система определяет, какие серверы нужны
3. **Кэширование решений**: Система запоминает паттерны использования для ускорения работы

**Критерии приемки**:
- Система активирует только релевантные серверы для каждой задачи
- Количество активных tools в контексте минимизировано

### BR-3: Прозрачность работы системы
**Описание**: Пользователь должен понимать, какие серверы активны и почему.

**Критерии приемки**:
- Пользователь может видеть список активных серверов
- Система логирует причины активации/деактивации серверов
- Доступна информация о том, какие tools доступны от каждого сервера

### BR-4: Масштабируемость
**Описание**: Поддержка неограниченного количества MCP серверов из Docker MCP Catalog.

**Критерии приемки**:
- Система работает с любым количеством серверов из Docker MCP Catalog
- Производительность не деградирует при увеличении количества доступных серверов
- Поддержка динамического добавления новых серверов без перезапуска системы

### BR-5: Надежность
**Описание**: Система должна работать стабильно даже при сбоях отдельных серверов.

**Критерии приемки**:
- Сбой одного сервера не влияет на работу других
- Система автоматически восстанавливается после сбоев
- Graceful degradation при недоступности серверов
- Логирование всех ошибок для диагностики

### BR-6: Производительность
**Описание**: Минимальная задержка при активации/деактивации серверов.

**Критерии приемки**:
- Время активации сервера < 3 секунд
- Время деактивации сервера < 1 секунды
- Общая задержка при переключении набора серверов < 5 секунд
- Система не блокирует работу пользователя во время переключения

---

## 🔧 Технические ограничения

### TC-1: Ограничение Cursor 2.x
**Описание**: Cursor 2.x требует перезапуска MCP соединения для обнаружения новых tools после активации сервера.

**Влияние**:
- Нельзя динамически добавлять tools без перезапуска соединения
- Необходимо реализовать механизм переподключения при изменении набора серверов

### TC-2: MCP Protocol ограничения
**Описание**: Стандартный протокол MCP не поддерживает динамическое добавление tools без перезапуска соединения.

**Влияние**:
- Невозможно добавить tools "на лету"
- Необходимо полное переподключение для обновления списка tools

### TC-3: Docker MCP Toolkit CLI
**Описание**: Управление серверами происходит через CLI команды `docker mcp`.

**Влияние**:
- Зависимость от доступности Docker MCP Toolkit
- Необходимость парсинга вывода CLI команд
- Асинхронное выполнение команд может создавать задержки

**Решение**:
- Использовать Docker MCP Toolkit API (если доступен) или надежный парсинг CLI вывода
- Реализовать очередь команд для управления асинхронными операциями
- Добавить retry-логику для обработки временных сбоев

### TC-4: Token Budget
**Описание**: Критически важно минимизировать количество токенов в контексте.

**Влияние**:
- Каждый tool в контексте потребляет токены
- Большое количество tools может привести к превышению лимитов
- Необходимо балансировать между функциональностью и размером контекста

## 📚 Дополнительные ресурсы

- [Полный список команд Docker MCP Toolkit](./docker_mcp_commands.md)
- [Docker MCP Toolkit Documentation](https://docs.docker.com/desktop/mcp/)

---

## 🚀 Быстрый старт

1. Установите Docker Desktop с MCP Toolkit
2. Добавьте и настройте список доступных серверов MCP серверы в Docker MCP из Catalog Docker Desktop MCP Toolkit
3. Добавьте `docker-mcp-orchestrator` в ваш AI-клиент
4. Начните работать — система будет автоматически управлять серверами!

---
