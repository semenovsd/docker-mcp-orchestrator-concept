# Docker MCP Orchestrator - Архитектура решения

## 📋 Содержание

1. [Обзор архитектуры](#обзор-архитектуры)
2. [Ключевая архитектурная проблема](#ключевая-архитектурная-проблема)
3. [Предлагаемая архитектура](#предлагаемая-архитектура)
4. [Компоненты системы](#компоненты-системы)
5. [Альтернативные подходы](#альтернативные-подходы)
6. [Детальная архитектура компонентов](#детальная-архитектура-компонентов)
7. [Потоки данных](#потоки-данных)
8. [Стратегии оптимизации](#стратегии-оптимизации)
9. [Обработка ошибок и надежность](#обработка-ошибок-и-надежность)
10. [Масштабируемость](#масштабируемость)
11. [Безопасность](#безопасность)
12. [Мониторинг и наблюдаемость](#мониторинг-и-наблюдаемость)

---

## Обзор архитектуры

### Цель системы

Docker MCP Orchestrator — это интеллектуальная система управления жизненным циклом MCP серверов, которая автоматически определяет и активирует только необходимые серверы для каждой задачи пользователя, минимизируя контекст AI и оптимизируя производительность.

### Архитектурные принципы

1. **Минимализм контекста**: Система предоставляет минимальный набор tools для определения нужных серверов
2. **Интеллектуальный выбор**: Автоматическое определение релевантных серверов на основе анализа запроса
3. **Прозрачность**: Пользователь всегда понимает, какие серверы активны и почему
4. **Надежность**: Graceful degradation при сбоях отдельных компонентов
5. **Производительность**: Минимальные задержки при переключении серверов
6. **Масштабируемость**: Поддержка неограниченного количества серверов

---

## Ключевая архитектурная проблема

### Проблема "курицы и яйца"

**Суть проблемы**: 
- Для определения нужных MCP серверов необходимо проанализировать запрос пользователя
- Для анализа нужны tools от Orchestrator
- Orchestrator должен предоставить минимальный набор tools для определения серверов
- После активации серверов требуется переподключение MCP соединения (ограничение Cursor 2.x)
- Новые tools становятся доступны только после переподключения

**Ограничения**:
- Cursor 2.x требует перезапуска MCP соединения для обнаружения новых tools
- Стандартный MCP протокол не поддерживает динамическое добавление tools
- Критически важно минимизировать количество токенов в контексте

---

## Предлагаемая архитектура

### Архитектурный подход: "Метаданные + Интеллектуальный классификатор"

Система использует **двухфазный подход**:

1. **Фаза анализа** (Phase 1): Orchestrator предоставляет минимальный набор tools для анализа запроса и определения нужных серверов
2. **Фаза активации** (Phase 2): Orchestrator активирует выбранные серверы, переподключается и предоставляет tools от активных серверов

### Высокоуровневая архитектура

```
┌─────────────────────────────────────────────────────────────────┐
│                        AI Client (Cursor)                        │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ MCP Protocol (stdio/SSE)
                             │
┌────────────────────────────▼────────────────────────────────────┐
│              Docker MCP Orchestrator (MCP Server)                │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  Phase 1: Request Analysis & Server Selection           │   │
│  │  ┌──────────────────┐  ┌──────────────────────────────┐ │   │
│  │  │ Request Analyzer │  │ Server Selector              │ │   │
│  │  │ (LLM-based)      │→ │ (Metadata + Classification) │ │   │
│  │  └──────────────────┘  └──────────────────────────────┘ │   │
│  └──────────────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  Phase 2: Server Lifecycle Management                    │   │
│  │  ┌──────────────────┐  ┌──────────────────────────────┐ │   │
│  │  │ Server Manager   │  │ Connection Manager           │ │   │
│  │  │ (Docker MCP CLI) │→ │ (Reconnection Handler)       │ │   │
│  │  └──────────────────┘  └──────────────────────────────┘ │   │
│  └──────────────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  Metadata Registry                                        │   │
│  │  ┌──────────────────┐  ┌──────────────────────────────┐ │   │
│  │  │ Server Metadata  │  │ Tool Catalog                 │ │   │
│  │  │ (Capabilities)   │  │ (Tool Descriptions)          │ │   │
│  │  └──────────────────┘  └──────────────────────────────┘ │   │
│  └──────────────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  Cache & State Management                                │   │
│  │  ┌──────────────────┐  ┌──────────────────────────────┐ │   │
│  │  │ Pattern Cache    │  │ State Store                  │ │   │
│  │  │ (Request→Servers)│  │ (Active Servers)             │ │   │
│  │  └──────────────────┘  └──────────────────────────────┘ │   │
│  └──────────────────────────────────────────────────────────┘   │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ Docker MCP Toolkit CLI
                             │
┌────────────────────────────▼────────────────────────────────────┐
│                    Docker MCP Toolkit                            │
│  ┌──────────────────┐  ┌──────────────────────────────┐        │
│  │ Server Gateway   │  │ Catalog Manager               │        │
│  │ (Container Mgmt) │  │ (Server Registry)             │        │
│  └──────────────────┘  └──────────────────────────────┘        │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ Docker API
                             │
┌────────────────────────────▼────────────────────────────────────┐
│                    Docker Engine                                  │
│              (MCP Server Containers)                              │
└──────────────────────────────────────────────────────────────────┘
```

---

## Компоненты системы

### 1. Request Analyzer (Анализатор запросов)

**Назначение**: Анализирует запрос пользователя и извлекает ключевые интенты, домены и действия.

**Реализация**:
- Использует встроенный LLM (локальный или через API) для анализа запроса
- Извлекает семантические признаки: домены (GitHub, PostgreSQL, File System), действия (read, write, query), контекст (project, database, repository)

**Входные данные**:
- Текстовый запрос пользователя
- Контекст предыдущих запросов (опционально)

**Выходные данные**:
- Структурированное представление запроса:
  - Домены: `["github", "filesystem"]`
  - Действия: `["read", "write"]`
  - Контекст: `{"project": "my-app", "type": "backend"}`
  - Приоритет: `high/medium/low`

**Оптимизация**:
- Кэширование результатов анализа для похожих запросов
- Использование легковесных моделей для быстрого анализа
- Batch-обработка для множественных запросов

---

### 2. Server Selector (Селектор серверов)

**Назначение**: Определяет оптимальный набор MCP серверов на основе анализа запроса и метаданных серверов.

**Алгоритм выбора**:

```
1. Получить метаданные всех доступных серверов из Metadata Registry
2. Сопоставить признаки запроса с capabilities серверов
3. Применить правила приоритизации:
   - Обязательные серверы (если указаны в запросе)
   - Серверы с высоким релевантным score
   - Минимизация количества серверов при сохранении функциональности
4. Проверить зависимости между серверами
5. Вернуть финальный список серверов для активации
```

**Метрики релевантности**:
- **Semantic Match**: Совпадение доменов и действий
- **Tool Coverage**: Покрытие требуемых операций
- **Historical Success**: Успешность использования в похожих запросах
- **Performance Score**: Скорость работы и надежность сервера

**Выходные данные**:
- Список серверов для активации: `["github-mcp", "filesystem-mcp"]`
- Список серверов для деактивации: `["postgres-mcp"]` (если были активны)
- Обоснование выбора (для прозрачности)

---

### 3. Metadata Registry (Реестр метаданных)

**Назначение**: Хранит структурированные метаданные о всех доступных MCP серверах.

**Структура метаданных сервера**:

```yaml
server:
  name: "github-mcp"
  display_name: "GitHub MCP Server"
  description: "Git operations, repository management"
  
  capabilities:
    domains: ["github", "git", "repository", "version-control"]
    actions: ["read", "write", "create", "delete", "merge", "branch"]
    resource_types: ["repository", "issue", "pull-request", "commit"]
  
  tools:
    - name: "github_get_repository"
      description: "Get repository information"
      domain: "github"
      action: "read"
    - name: "github_create_branch"
      description: "Create a new branch"
      domain: "github"
      action: "write"
  
  performance:
    startup_time: 2.5s
    avg_response_time: 0.3s
    reliability: 0.99
  
  dependencies: []
  conflicts: []
  
  tags: ["development", "collaboration", "version-control"]
```

**Источники метаданных**:
1. **Docker MCP Catalog**: Автоматическое извлечение из каталога
2. **Server Inspection**: Анализ через `docker mcp server inspect`
3. **Tool Discovery**: Парсинг tools через `docker mcp tools ls --format json`
4. **User Configuration**: Ручная настройка и дополнение метаданных

**Обновление метаданных**:
- Автоматическое обновление при изменении каталога
- Периодическая синхронизация с Docker MCP Catalog
- Инкрементальное обновление при обнаружении новых серверов

---

### 4. Server Manager (Менеджер серверов)

**Назначение**: Управляет жизненным циклом MCP серверов через Docker MCP Toolkit CLI.

**Функциональность**:

**Активация серверов**:
```python
async def activate_servers(server_names: List[str]) -> ActivationResult:
    """
    1. Проверить текущее состояние серверов
    2. Определить серверы для активации (не активные)
    3. Выполнить: docker mcp server enable <server1> <server2> ...
    4. Дождаться готовности серверов (health check)
    5. Вернуть результат активации
    """
```

**Деактивация серверов**:
```python
async def deactivate_servers(server_names: List[str]) -> DeactivationResult:
    """
    1. Выполнить: docker mcp server disable <server1> <server2> ...
    2. Подтвердить остановку контейнеров
    3. Очистить ресурсы
    """
```

**Мониторинг состояния**:
- Отслеживание статуса серверов через `docker mcp server ls --json`
- Health checks для активных серверов
- Обнаружение сбоев и автоматическое восстановление

**Оптимизации**:
- Batch-операции для множественных серверов
- Параллельная активация независимых серверов
- Retry-логика с экспоненциальным backoff
- Timeout-обработка для зависших операций

---

### 5. Connection Manager (Менеджер соединений)

**Назначение**: Управляет переподключением MCP соединения при изменении набора активных серверов.

**Проблема**: Cursor 2.x требует перезапуска MCP соединения для обнаружения новых tools.

**Решение**:

**Стратегия переподключения**:

1. **Graceful Disconnect**:
   - Сохранить состояние текущей сессии
   - Завершить текущие операции (если возможно)
   - Отправить сигнал отключения клиенту

2. **Server Activation**:
   - Активировать новые серверы через Server Manager
   - Дождаться готовности всех серверов
   - Подтвердить доступность tools

3. **Reconnection**:
   - Инициировать переподключение (через сигнал клиенту или автоматически)
   - Восстановить состояние сессии
   - Уведомить пользователя о доступных tools

**Реализация**:

**Вариант A: Сигнал клиенту (рекомендуемый)**
- Orchestrator отправляет специальный tool `request_reconnection` клиенту
- Клиент обрабатывает сигнал и переподключается
- Новые tools становятся доступны

**Вариант B: Автоматическое переподключение**
- Orchestrator завершает текущее соединение
- Клиент автоматически переподключается (если поддерживается)
- Новые tools обнаруживаются при переподключении

**Вариант C: Lazy Loading (если поддерживается)**
- Orchestrator активирует серверы в фоне
- Tools становятся доступны без переподключения (будущее улучшение)

---

### 6. Pattern Cache (Кэш паттернов)

**Назначение**: Кэширует соответствия между запросами и наборами серверов для ускорения работы.

**Структура кэша**:

```python
CacheEntry:
    request_signature: str  # Хэш признаков запроса
    server_set: List[str]   # Набор серверов
    success_rate: float     # Процент успешных использований
    avg_response_time: float # Среднее время ответа
    last_used: datetime      # Время последнего использования
    usage_count: int         # Количество использований
```

**Стратегия кэширования**:

1. **Lookup**: Поиск похожих запросов в кэше
2. **Validation**: Проверка актуальности (не изменились ли серверы)
3. **Scoring**: Вычисление релевантности кэшированного результата
4. **Fallback**: Если кэш не подходит, выполнить полный анализ

**Инвалидация кэша**:
- При изменении метаданных серверов
- При добавлении/удалении серверов
- При низком success_rate кэшированного результата
- По истечении TTL (Time To Live)

**Оптимизация**:
- Использование семантического поиска для похожих запросов
- Приоритизация часто используемых паттернов
- Адаптивный размер кэша (LRU eviction)

---

### 7. State Store (Хранилище состояния)

**Назначение**: Отслеживает текущее состояние системы и активных серверов.

**Состояние системы**:

```python
SystemState:
    active_servers: List[str]           # Активные серверы
    server_status: Dict[str, ServerStatus]  # Статус каждого сервера
    last_activation: datetime           # Время последней активации
    connection_state: ConnectionState   # Состояние MCP соединения
    pending_operations: List[Operation] # Ожидающие операции
```

**Персистентность**:
- Сохранение состояния в файл (JSON/YAML)
- Восстановление состояния при перезапуске
- Синхронизация с реальным состоянием Docker MCP Toolkit

---

## Альтернативные подходы

### Подход 1: "Минимальный Orchestrator" (Текущий - Рекомендуемый)

**Концепция**: Orchestrator предоставляет минимальный набор tools для анализа и управления, основная функциональность делегируется активированным серверам.

**Преимущества**:
- ✅ Минимальный контекст (3-5 tools от Orchestrator)
- ✅ Простота реализации
- ✅ Низкая задержка анализа
- ✅ Легкое масштабирование

**Недостатки**:
- ⚠️ Требуется переподключение при изменении серверов
- ⚠️ Зависимость от качества метаданных

**Tools от Orchestrator**:
1. `analyze_request` - Анализ запроса и определение серверов
2. `activate_servers` - Активация указанных серверов
3. `get_active_servers` - Получение списка активных серверов
4. `get_server_metadata` - Получение метаданных сервера
5. `request_reconnection` - Запрос переподключения (для обновления tools)

---

### Подход 2: "Умный Proxy"

**Концепция**: Orchestrator выступает как прокси, который динамически маршрутизирует вызовы tools к соответствующим серверам.

**Преимущества**:
- ✅ Единая точка входа для всех tools
- ✅ Возможность динамической маршрутизации
- ✅ Централизованное логирование и мониторинг

**Недостатки**:
- ❌ Высокий overhead на каждый вызов tool
- ❌ Сложность реализации
- ❌ Потенциальные проблемы с производительностью
- ❌ Нарушение стандарта MCP протокола

**Вывод**: Не рекомендуется из-за сложности и overhead.

---

### Подход 3: "Предустановленные серверы"

**Концепция**: Все серверы всегда активны, Orchestrator только фильтрует доступные tools в контексте.

**Преимущества**:
- ✅ Нет необходимости в переподключении
- ✅ Простота реализации

**Недостатки**:
- ❌ Не решает проблему перегрузки контекста
- ❌ Высокое потребление ресурсов
- ❌ Все tools все равно попадают в контекст

**Вывод**: Не решает основную проблему.

---

### Подход 4: "Гибридный подход с категориями"

**Концепция**: Серверы группируются в категории (Development, Database, Cloud, etc.), Orchestrator активирует целые категории.

**Преимущества**:
- ✅ Упрощение выбора (меньше вариантов)
- ✅ Быстрее принятие решений
- ✅ Логическая группировка

**Недостатки**:
- ⚠️ Менее точный выбор (может активировать лишние серверы)
- ⚠️ Требуется настройка категорий

**Вывод**: Может быть полезен как дополнительная оптимизация для Подхода 1.

---

## Детальная архитектура компонентов

### Request Analyzer - Детальная архитектура

**Компоненты**:

1. **Text Preprocessor**:
   - Нормализация текста
   - Извлечение ключевых слов
   - Определение языка

2. **Intent Extractor**:
   - Использование LLM для извлечения интентов
   - Классификация по доменам
   - Определение действий

3. **Context Builder**:
   - Анализ предыдущих запросов
   - Построение контекста сессии
   - Определение приоритетов

**Пример работы**:

```
Запрос: "Создай новую ветку в репозитории my-project и добавь файл config.yaml"

Анализ:
- Домены: ["github", "filesystem"]
- Действия: ["create", "write"]
- Контекст: {"repository": "my-project", "file": "config.yaml"}
- Приоритет: high
- Требуемые операции: 
  - github: create_branch, create_file
  - filesystem: read_file, write_file
```

---

### Server Selector - Детальный алгоритм

**Алгоритм выбора серверов**:

```python
def select_servers(request_analysis: RequestAnalysis, 
                   metadata_registry: MetadataRegistry) -> ServerSelection:
    
    # Шаг 1: Фильтрация по доменам
    candidate_servers = filter_by_domains(
        metadata_registry.servers,
        request_analysis.domains
    )
    
    # Шаг 2: Оценка релевантности
    scored_servers = []
    for server in candidate_servers:
        score = calculate_relevance_score(
            server=server,
            request=request_analysis,
            historical_data=cache.get_patterns(server.name)
        )
        scored_servers.append((server, score))
    
    # Шаг 3: Сортировка по релевантности
    scored_servers.sort(key=lambda x: x[1], reverse=True)
    
    # Шаг 4: Выбор минимального набора
    selected_servers = select_minimal_set(
        scored_servers,
        coverage_threshold=0.95  # 95% покрытие требуемых операций
    )
    
    # Шаг 5: Проверка зависимостей
    selected_servers = resolve_dependencies(selected_servers)
    
    # Шаг 6: Проверка конфликтов
    selected_servers = resolve_conflicts(selected_servers)
    
    return ServerSelection(
        servers_to_activate=selected_servers,
        servers_to_deactivate=get_servers_to_deactivate(selected_servers),
        reasoning=generate_reasoning(selected_servers, request_analysis)
    )
```

**Функция оценки релевантности**:

```python
def calculate_relevance_score(server, request, historical_data) -> float:
    # Semantic match (0.0 - 1.0)
    semantic_score = calculate_semantic_match(
        server.capabilities.domains,
        request.domains
    )
    
    # Tool coverage (0.0 - 1.0)
    coverage_score = calculate_tool_coverage(
        server.tools,
        request.required_operations
    )
    
    # Historical success (0.0 - 1.0)
    historical_score = historical_data.success_rate if historical_data else 0.5
    
    # Performance score (0.0 - 1.0)
    performance_score = calculate_performance_score(
        server.performance.startup_time,
        server.performance.avg_response_time,
        server.performance.reliability
    )
    
    # Взвешенная сумма
    total_score = (
        0.4 * semantic_score +
        0.3 * coverage_score +
        0.2 * historical_score +
        0.1 * performance_score
    )
    
    return total_score
```

---

### Metadata Registry - Детальная структура

**Источники метаданных**:

1. **Docker MCP Catalog** (`docker mcp catalog show --format json`):
   - Базовая информация о серверах
   - Конфигурация и параметры
   - OCI-образы

2. **Server Inspection** (`docker mcp server inspect`):
   - Детальная информация о сервере
   - Доступные ресурсы
   - Конфигурационные параметры

3. **Tool Discovery** (`docker mcp tools ls --format json`):
   - Список всех tools
   - Описания и параметры tools
   - Категоризация tools

4. **Runtime Analysis**:
   - Мониторинг использования tools
   - Сбор статистики производительности
   - Обнаружение паттернов использования

**Процесс обновления метаданных**:

```
1. Периодическая синхронизация (каждые 24 часа):
   - docker mcp catalog update
   - docker mcp catalog show --format json
   - Парсинг и обновление метаданных

2. При обнаружении нового сервера:
   - docker mcp server inspect <server>
   - docker mcp tools ls --format json --server <server>
   - Извлечение метаданных и добавление в реестр

3. Инкрементальное обновление:
   - Мониторинг изменений в каталоге
   - Обновление только измененных серверов
   - Валидация целостности метаданных
```

**Хранение метаданных**:

- **Формат**: JSON/YAML файлы в `~/.docker/mcp/orchestrator/metadata/`
- **Структура**: Один файл на сервер + индексный файл
- **Версионирование**: Хранение версий метаданных для отката
- **Кэширование**: In-memory кэш для быстрого доступа

---

### Server Manager - Детальная реализация

**Жизненный цикл сервера**:

```
1. IDLE → ACTIVATING
   - Вызов: docker mcp server enable <server>
   - Ожидание: Старт контейнера
   - Timeout: 10 секунд

2. ACTIVATING → HEALTH_CHECK
   - Проверка: docker mcp server ls --json
   - Валидация: Сервер в списке enabled
   - Health check: docker mcp tools ls --server <server>

3. HEALTH_CHECK → ACTIVE
   - Подтверждение: Tools доступны
   - Регистрация: Добавление в active_servers
   - Уведомление: Логирование активации

4. ACTIVE → DEACTIVATING
   - Вызов: docker mcp server disable <server>
   - Ожидание: Остановка контейнера

5. DEACTIVATING → IDLE
   - Подтверждение: Сервер не в списке enabled
   - Очистка: Удаление из active_servers
```

**Обработка ошибок**:

```python
async def activate_server_with_retry(server_name: str, max_retries: int = 3):
    for attempt in range(max_retries):
        try:
            result = await activate_server(server_name)
            if result.success:
                return result
        except ServerStartupTimeout:
            if attempt < max_retries - 1:
                await asyncio.sleep(2 ** attempt)  # Exponential backoff
                continue
            raise
        except ServerHealthCheckFailed:
            # Логирование и graceful degradation
            log_error(f"Server {server_name} failed health check")
            return ActivationResult(success=False, error="Health check failed")
```

**Параллельная активация**:

```python
async def activate_servers_parallel(server_names: List[str]):
    # Группировка по зависимостям
    dependency_groups = resolve_dependencies(server_names)
    
    # Последовательная активация групп
    for group in dependency_groups:
        # Параллельная активация внутри группы
        tasks = [activate_server(name) for name in group]
        results = await asyncio.gather(*tasks, return_exceptions=True)
        
        # Обработка результатов
        for result in results:
            if isinstance(result, Exception):
                handle_activation_error(result)
```

---

### Connection Manager - Детальная стратегия

**Стратегия переподключения**:

**Вариант A: Сигнал клиенту (Рекомендуемый)**

```python
async def handle_server_change(new_servers: List[str], old_servers: List[str]):
    # 1. Сохранение состояния
    session_state = save_current_session_state()
    
    # 2. Активация новых серверов
    activation_result = await server_manager.activate_servers(new_servers)
    if not activation_result.success:
        return handle_activation_failure(activation_result)
    
    # 3. Деактивация старых серверов
    deactivation_result = await server_manager.deactivate_servers(old_servers)
    
    # 4. Подтверждение готовности
    await verify_servers_ready(new_servers)
    
    # 5. Отправка сигнала переподключения
    await send_reconnection_signal(
        reason="Server set changed",
        new_servers=new_servers,
        session_state=session_state
    )
```

**Tool для переподключения**:

```python
@tool
async def request_reconnection(
    reason: str,
    new_servers: List[str],
    session_state: Optional[Dict] = None
) -> ReconnectionResult:
    """
    Запрашивает переподключение MCP соединения для обновления списка доступных tools.
    
    Args:
        reason: Причина переподключения (например, "Server set changed")
        new_servers: Список серверов, которые будут доступны после переподключения
        session_state: Сохраненное состояние сессии (опционально)
    
    Returns:
        ReconnectionResult с информацией о статусе переподключения
    """
    # Логирование
    logger.info(f"Reconnection requested: {reason}")
    logger.info(f"New servers: {new_servers}")
    
    # Возврат инструкций для клиента
    return ReconnectionResult(
        action="reconnect",
        message=f"Please reconnect to see tools from: {', '.join(new_servers)}",
        new_servers=new_servers,
        session_state=session_state
    )
```

**Вариант B: Автоматическое переподключение (Если поддерживается)**

```python
async def handle_server_change_auto(new_servers: List[str], old_servers: List[str]):
    # 1. Graceful shutdown текущего соединения
    await graceful_shutdown()
    
    # 2. Активация серверов
    await server_manager.activate_servers(new_servers)
    await server_manager.deactivate_servers(old_servers)
    
    # 3. Автоматическое переподключение
    # (требует поддержки со стороны клиента)
    await auto_reconnect()
```

---

## Потоки данных

### Поток 1: Первичный анализ запроса

```
User Request
    ↓
[Request Analyzer]
    ├─→ Text Preprocessing
    ├─→ Intent Extraction (LLM)
    └─→ Context Building
    ↓
RequestAnalysis {
    domains: ["github", "filesystem"],
    actions: ["create", "write"],
    context: {...},
    priority: "high"
}
    ↓
[Pattern Cache]
    ├─→ Lookup similar requests
    └─→ Return cached result (if found)
    ↓
[Server Selector]
    ├─→ Load metadata from Metadata Registry
    ├─→ Calculate relevance scores
    ├─→ Select minimal server set
    └─→ Generate reasoning
    ↓
ServerSelection {
    servers_to_activate: ["github-mcp", "filesystem-mcp"],
    servers_to_deactivate: ["postgres-mcp"],
    reasoning: "..."
}
```

### Поток 2: Активация серверов и переподключение

```
ServerSelection
    ↓
[Server Manager]
    ├─→ Check current server state
    ├─→ Activate new servers (docker mcp server enable)
    ├─→ Deactivate old servers (docker mcp server disable)
    └─→ Health check active servers
    ↓
ActivationResult {
    success: true,
    activated: ["github-mcp", "filesystem-mcp"],
    deactivated: ["postgres-mcp"],
    errors: []
}
    ↓
[Connection Manager]
    ├─→ Save session state
    ├─→ Verify servers ready
    └─→ Send reconnection signal
    ↓
[AI Client]
    ├─→ Handle reconnection signal
    ├─→ Reconnect to MCP
    └─→ Discover new tools
    ↓
User sees new tools from activated servers
```

### Поток 3: Обновление метаданных

```
Scheduled Update / New Server Detected
    ↓
[Metadata Registry]
    ├─→ docker mcp catalog update
    ├─→ docker mcp catalog show --format json
    ├─→ Parse catalog data
    └─→ For each server:
        ├─→ docker mcp server inspect <server>
        ├─→ docker mcp tools ls --format json --server <server>
        ├─→ Extract metadata
        └─→ Update registry
    ↓
Metadata Updated
    ├─→ Invalidate Pattern Cache
    └─→ Notify Server Selector
```

---

## Стратегии оптимизации

### 1. Минимизация контекста Orchestrator

**Цель**: Предоставить минимальный набор tools от Orchestrator.

**Стратегия**:

**Минимальный набор tools (3-5 tools)**:

1. `analyze_and_activate_servers` (основной tool):
   - Анализирует запрос
   - Определяет нужные серверы
   - Активирует их
   - Инициирует переподключение
   - Все в одном вызове для минимизации токенов

2. `get_active_servers`:
   - Возвращает список активных серверов
   - Для прозрачности и отладки

3. `get_server_metadata`:
   - Возвращает метаданные сервера
   - Для информирования пользователя

4. `manual_server_control` (опционально):
   - Ручное управление серверами
   - Для продвинутых пользователей

**Оптимизация описаний tools**:
- Краткие, но информативные описания
- Минимум примеров в описаниях
- Использование стандартных терминов

---

### 2. Кэширование и предсказание

**Стратегии кэширования**:

1. **Pattern Cache**:
   - Кэширование соответствий запрос→серверы
   - Семантический поиск похожих запросов
   - Адаптивный TTL

2. **Metadata Cache**:
   - In-memory кэш метаданных серверов
   - Инвалидация при изменении каталога
   - Lazy loading для редко используемых серверов

3. **Server State Cache**:
   - Кэширование состояния серверов
   - Периодическая синхронизация с Docker MCP Toolkit
   - Быстрый доступ без CLI вызовов

**Предсказание**:

1. **Prefetching**:
   - Предзагрузка часто используемых серверов
   - На основе исторических данных
   - В фоновом режиме

2. **Predictive Activation**:
   - Активация серверов на основе контекста сессии
   - Анализ паттернов использования
   - Адаптивное обучение

---

### 3. Оптимизация производительности

**Параллелизация**:

1. **Параллельная активация независимых серверов**:
   ```python
   # Группировка по зависимостям
   independent_groups = group_by_dependencies(servers)
   
   # Параллельная активация внутри групп
   for group in independent_groups:
       await asyncio.gather(*[activate(s) for s in group])
   ```

2. **Асинхронная обработка**:
   - Все операции с Docker MCP Toolkit асинхронные
   - Неблокирующие вызовы CLI
   - Параллельная обработка множественных запросов

**Оптимизация CLI вызовов**:

1. **Batch операции**:
   - `docker mcp server enable server1 server2 server3` (одна команда)
   - Вместо трех отдельных команд

2. **Кэширование результатов**:
   - Кэширование вывода `docker mcp server ls --json`
   - Обновление при изменениях
   - Минимизация CLI вызовов

3. **Connection Pooling** (если доступно):
   - Переиспользование соединений с Docker
   - Минимизация overhead на установку соединения

---

### 4. Оптимизация анализа запросов

**Легковесный анализ**:

1. **Правила вместо LLM** (для простых случаев):
   - Ключевые слова → домены
   - Шаблоны запросов → серверы
   - Быстрый путь для типичных запросов

2. **Гибридный подход**:
   - Сначала проверка правил
   - Если не найдено → использование LLM
   - Кэширование результатов LLM

3. **Streaming анализ** (для длинных запросов):
   - Начальный анализ на первых словах
   - Уточнение по мере поступления текста
   - Инкрементальное обновление выбора серверов

---

## Обработка ошибок и надежность

### Стратегия обработки ошибок

**Уровни обработки ошибок**:

1. **Уровень анализа запроса**:
   - Fallback на правила, если LLM недоступен
   - Использование кэша при ошибках анализа
   - Graceful degradation до базового набора серверов

2. **Уровень выбора серверов**:
   - Проверка доступности серверов перед выбором
   - Альтернативные серверы при недоступности основных
   - Минимальный набор серверов при ошибках

3. **Уровень активации серверов**:
   - Retry с экспоненциальным backoff
   - Timeout обработка
   - Частичная активация (активировать доступные, пропустить недоступные)
   - Уведомление пользователя о проблемах

4. **Уровень переподключения**:
   - Повторные попытки переподключения
   - Сохранение состояния при ошибках
   - Восстановление после сбоев

**Типы ошибок и обработка**:

| Тип ошибки | Обработка | Влияние на пользователя |
|------------|-----------|------------------------|
| Server startup timeout | Retry 3 раза, затем skip | Предупреждение, работа с доступными серверами |
| Server health check failed | Deactivate, попробовать альтернативу | Уведомление, использование альтернативных серверов |
| Docker MCP Toolkit недоступен | Кэшированное состояние, уведомление | Предупреждение, ограниченная функциональность |
| Metadata registry поврежден | Восстановление из backup, fallback на базовые метаданные | Временное снижение точности выбора |
| Connection reconnection failed | Повторные попытки, ручной режим | Инструкции для ручного переподключения |

**Circuit Breaker Pattern**:

```python
class ServerActivationCircuitBreaker:
    def __init__(self, failure_threshold=5, timeout=60):
        self.failure_count = 0
        self.failure_threshold = failure_threshold
        self.timeout = timeout
        self.last_failure_time = None
        self.state = "CLOSED"  # CLOSED, OPEN, HALF_OPEN
    
    async def activate(self, server_name: str):
        if self.state == "OPEN":
            if time.time() - self.last_failure_time > self.timeout:
                self.state = "HALF_OPEN"
            else:
                raise CircuitBreakerOpenError()
        
        try:
            result = await activate_server(server_name)
            if self.state == "HALF_OPEN":
                self.state = "CLOSED"
                self.failure_count = 0
            return result
        except Exception as e:
            self.failure_count += 1
            self.last_failure_time = time.time()
            if self.failure_count >= self.failure_threshold:
                self.state = "OPEN"
            raise
```

**Graceful Degradation**:

1. **Минимальный набор серверов**:
   - При ошибках активации, активировать только критически важные
   - Продолжить работу с ограниченной функциональностью

2. **Fallback на ручной режим**:
   - При критических ошибках, переключение на ручное управление
   - Уведомление пользователя о проблемах

3. **Кэшированное состояние**:
   - Использование последнего известного состояния при недоступности Docker MCP Toolkit
   - Предупреждение о возможной устарелости данных

---

## Масштабируемость

### Горизонтальное масштабирование

**Текущая архитектура**: Single-instance (один экземпляр Orchestrator)

**Ограничения**:
- Один Orchestrator на одного пользователя
- Нет необходимости в горизонтальном масштабировании для текущего use case

**Будущие улучшения** (если потребуется):
- Поддержка множественных пользователей
- Shared state между экземплярами
- Load balancing для анализа запросов

### Вертикальное масштабирование

**Оптимизации для больших каталогов серверов**:

1. **Индексирование метаданных**:
   - Индекс по доменам для быстрого поиска
   - Индекс по actions для фильтрации
   - Полнотекстовый поиск по описаниям

2. **Lazy Loading**:
   - Загрузка метаданных только для релевантных серверов
   - Отложенная загрузка детальной информации
   - Кэширование часто используемых метаданных

3. **Партиционирование**:
   - Группировка серверов по категориям
   - Поиск только в релевантных категориях
   - Параллельная обработка категорий

**Производительность при масштабировании**:

| Количество серверов | Время анализа | Время активации | Память |
|---------------------|---------------|-----------------|--------|
| 10 серверов         | < 100ms       | < 3s            | < 50MB |
| 50 серверов         | < 200ms       | < 5s            | < 100MB |
| 100 серверов        | < 500ms       | < 8s            | < 200MB |
| 500+ серверов       | < 1s          | < 15s           | < 500MB |

*Примечание: Время активации зависит от количества активируемых серверов, а не от общего количества доступных.*

---

## Безопасность

### Уровни безопасности

1. **Уровень доступа к серверам**:
   - Валидация списка доступных серверов
   - Проверка прав доступа перед активацией
   - Изоляция серверов в Docker контейнерах

2. **Уровень метаданных**:
   - Валидация метаданных перед использованием
   - Проверка целостности метаданных
   - Защита от подделки метаданных

3. **Уровень CLI команд**:
   - Санитизация входных данных для CLI команд
   - Защита от injection атак
   - Валидация параметров команд

4. **Уровень состояния**:
   - Шифрование чувствительных данных в состоянии
   - Защита файлов состояния
   - Аудит изменений состояния

### Рекомендации по безопасности

1. **Валидация входных данных**:
   ```python
   def validate_server_name(server_name: str) -> bool:
       # Проверка формата имени
       if not re.match(r'^[a-z0-9-]+$', server_name):
           return False
       # Проверка длины
       if len(server_name) > 100:
           return False
       return True
   ```

2. **Sandboxing**:
   - Все CLI команды выполняются в изолированном окружении
   - Ограничение ресурсов для Docker контейнеров
   - Мониторинг подозрительной активности

3. **Аудит**:
   - Логирование всех операций активации/деактивации
   - Трекинг изменений состояния
   - Алерты при аномальной активности

---

## Мониторинг и наблюдаемость

### Метрики системы

**Ключевые метрики**:

1. **Производительность**:
   - Время анализа запроса (p50, p95, p99)
   - Время активации сервера (p50, p95, p99)
   - Время переподключения
   - Общее время обработки запроса

2. **Надежность**:
   - Процент успешных активаций
   - Процент успешных переподключений
   - Количество ошибок по типам
   - Uptime системы

3. **Использование**:
   - Количество активных серверов
   - Частота переключения серверов
   - Популярность серверов
   - Эффективность кэша (hit rate)

4. **Качество**:
   - Точность выбора серверов (user feedback)
   - Процент случаев, когда нужные серверы были активированы
   - Количество ручных корректировок

### Логирование

**Уровни логирования**:

1. **DEBUG**: Детальная информация для отладки
   - Все шаги анализа запроса
   - Детали выбора серверов
   - Состояние кэша

2. **INFO**: Информационные сообщения
   - Активация/деактивация серверов
   - Переподключения
   - Обновления метаданных

3. **WARNING**: Предупреждения
   - Частичные ошибки активации
   - Устаревшие метаданные
   - Низкая эффективность кэша

4. **ERROR**: Ошибки
   - Критические ошибки активации
   - Ошибки переподключения
   - Повреждение метаданных

**Структурированное логирование**:

```json
{
  "timestamp": "2024-01-15T10:30:00Z",
  "level": "INFO",
  "component": "ServerManager",
  "action": "activate_servers",
  "servers": ["github-mcp", "filesystem-mcp"],
  "duration_ms": 2500,
  "success": true,
  "context": {
    "request_id": "req-123",
    "user_id": "user-456"
  }
}
```

### Трейсинг

**Распределенный трейсинг** (для будущих улучшений):

- Трейсинг запросов через все компоненты
- Измерение времени в каждом компоненте
- Идентификация узких мест
- Визуализация потоков данных

---

## Рекомендации по реализации

### Фазы разработки

**Фаза 1: MVP (Минимально жизнеспособный продукт)**
- Базовый Request Analyzer с правилами
- Простой Server Selector с метаданными
- Базовая активация/деактивация серверов
- Простое переподключение через сигнал клиенту
- Минимальный набор tools (3-5)

**Фаза 2: Оптимизация**
- Интеграция LLM для анализа запросов
- Pattern Cache для ускорения
- Параллельная активация серверов
- Улучшенная обработка ошибок

**Фаза 3: Интеллектуальные возможности**
- Предсказательная активация
- Адаптивное обучение на основе использования
- Автоматическая оптимизация метаданных
- Расширенная аналитика

### Технологический стек (рекомендации)

**Язык программирования**: Python 3.11+

**Основные библиотеки**:
- **MCP SDK**: Для реализации MCP сервера
- **asyncio**: Для асинхронных операций
- **Docker SDK**: Для взаимодействия с Docker (опционально, если доступен API)
- **Pydantic**: Для валидации данных и моделей
- **SQLite/JSON**: Для хранения состояния и кэша

**LLM для анализа** (опционально):
- **OpenAI API**: Для анализа запросов (если доступен)
- **Local LLM**: Для офлайн работы (Ollama, LM Studio)
- **Hybrid**: Правила + LLM для баланса скорости и точности

**Инструменты разработки**:
- **pytest**: Для тестирования
- **mypy**: Для статической типизации
- **black**: Для форматирования кода
- **ruff**: Для линтинга

---

## Альтернативные архитектурные решения

### Альтернатива 1: "Event-Driven Architecture"

**Концепция**: Использование событийной архитектуры для асинхронной обработки изменений.

**Преимущества**:
- ✅ Высокая отзывчивость
- ✅ Лучшая масштабируемость
- ✅ Разделение ответственности

**Недостатки**:
- ⚠️ Усложнение архитектуры
- ⚠️ Сложность отладки
- ⚠️ Overhead для текущего use case

**Вывод**: Избыточно для текущих требований, но может быть полезно в будущем.

---

### Альтернатива 2: "Plugin Architecture"

**Концепция**: Расширяемая архитектура с плагинами для различных стратегий выбора серверов.

**Преимущества**:
- ✅ Гибкость и расширяемость
- ✅ Возможность кастомизации
- ✅ Легкое добавление новых стратегий

**Недостатки**:
- ⚠️ Усложнение для пользователей
- ⚠️ Необходимость выбора стратегии

**Вывод**: Может быть полезно как опциональная функция для продвинутых пользователей.

---

### Альтернатива 3: "ML-Based Server Selection"

**Концепция**: Использование машинного обучения для выбора серверов на основе исторических данных.

**Преимущества**:
- ✅ Высокая точность при наличии данных
- ✅ Адаптация к паттернам использования
- ✅ Персонализация

**Недостатки**:
- ⚠️ Требует большого объема данных
- ⚠️ Сложность обучения и поддержки
- ⚠️ Overhead для начального использования

**Вывод**: Полезно как долгосрочное улучшение, но не для MVP.

---

## Заключение

### Рекомендуемая архитектура

**Основной подход**: "Метаданные + Интеллектуальный классификатор" с двухфазным процессом:
1. Фаза анализа с минимальным набором tools
2. Фаза активации с переподключением

**Ключевые компоненты**:
1. Request Analyzer (LLM-based с fallback на правила)
2. Server Selector (на основе метаданных и релевантности)
3. Metadata Registry (автоматическое обновление из Docker MCP Catalog)
4. Server Manager (управление через Docker MCP Toolkit CLI)
5. Connection Manager (обработка переподключений)
6. Pattern Cache (ускорение работы)
7. State Store (отслеживание состояния)

**Критические решения**:
- Минимальный набор tools от Orchestrator (3-5 tools)
- Переподключение через сигнал клиенту
- Кэширование для производительности
- Graceful degradation при ошибках

**Приоритеты реализации**:
1. MVP с базовой функциональностью
2. Оптимизация производительности
3. Интеллектуальные возможности

Эта архитектура обеспечивает баланс между функциональностью, производительностью и простотой реализации, эффективно решая основную проблему перегрузки контекста AI при работе с множественными MCP серверами.
