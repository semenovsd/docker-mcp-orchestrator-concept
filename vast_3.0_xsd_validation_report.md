# Отчет о валидации VAST 3.0 документа и проверке соответствия XSD схеме

## 🔍 Проверка на соответствие спецификации VAST 3.0

### ❌ КРИТИЧЕСКИЕ ОШИБКИ

#### 1. Отсутствует обязательный атрибут `version` у `<AdSystem>`

**Текущее:**
```xml
<AdSystem>Rollover</AdSystem>
```

**Требование VAST 3.0:**
В спецификации VAST 3.0 атрибут `version` у элемента `<AdSystem>` является **ОБЯЗАТЕЛЬНЫМ** (required).

**Должно быть:**
```xml
<AdSystem version="1.0">Rollover</AdSystem>
```

**Критичность:** 🔴 КРИТИЧЕСКАЯ - нарушение обязательного требования VAST 3.0

**Ссылка на спецификацию:** Согласно VAST 3.0, элемент `<AdSystem>` должен иметь атрибут `version` для идентификации версии системы показа рекламы.

---

### 2. Отсутствует элемент `<VideoClicks>` (опционально, но рекомендуется)

**Текущее:**
Элемент `<VideoClicks>` полностью отсутствует в документе.

**Требование VAST 3.0:**
Элемент `<VideoClicks>` опционален, но **настоятельно рекомендуется** для обеспечения интерактивности рекламы.

**Статус:** 🟡 РЕКОМЕНДАЦИЯ - не критично, но желательно добавить

---

## ⚠️ ПРЕДУПРЕЖДЕНИЯ И РЕКОМЕНДАЦИИ

### 3. Формат `Duration` без миллисекунд

**Текущее:**
```xml
<Duration>00:00:15</Duration>
```

**Требование VAST 3.0:**
Формат `HH:MM:SS` корректен, но рекомендуется использовать формат с миллисекундами `HH:MM:SS.mmm` для большей точности.

**Рекомендуется:**
```xml
<Duration>00:00:15.000</Duration>
```

**Статус:** 🟡 РЕКОМЕНДАЦИЯ

---

### 4. Формат `skipoffset` без миллисекунд

**Текущее:**
```xml
<Linear skipoffset="00:00:15">
```

**Требование VAST 3.0:**
Формат `HH:MM:SS` корректен, но рекомендуется использовать формат с миллисекундами или процент.

**Рекомендации:**
- С миллисекундами: `skipoffset="00:00:15.000"`
- Или в процентах: `skipoffset="25%"` (если нужно пропустить после 25% ролика)

**Статус:** 🟡 РЕКОМЕНДАЦИЯ

---

### 5. Логическая проблема с `skipoffset`

**Наблюдение:**
- Длительность ролика: `00:00:15`
- Skipoffset: `00:00:15`

**Проблема:**
Если `skipoffset` равен длительности ролика, кнопка пропуска будет недоступна, так как ролик уже закончится к моменту, когда пропуск станет доступен.

**Рекомендация:**
Использовать процентное значение, например `skipoffset="25%"` для пропуска после первой четверти.

**Статус:** 🟡 РЕКОМЕНДАЦИЯ

---

## ✅ ПРАВИЛЬНО РЕАЛИЗОВАННЫЕ ЭЛЕМЕНТЫ

### Структура документа
- ✅ Корневой элемент `<VAST>` с версией `3.0`
- ✅ Правильное объявление namespace `xmlns:xsi`
- ✅ Правильная иерархия: VAST → Ad → InLine

### Обязательные элементы
- ✅ `<Ad>` с атрибутом `id`
- ✅ `<InLine>`
- ✅ `<AdTitle>` с содержимым
- ✅ `<Description>` присутствует
- ✅ `<Error>` с макросом `[ERRORCODE]`
- ✅ `<Impression>` (минимум один, присутствует два)
- ✅ `<Creatives>`
- ✅ `<Creative>` с атрибутом `id`
- ✅ `<Linear>`
- ✅ `<Duration>` в правильном формате
- ✅ `<TrackingEvents>` с корректными событиями
- ✅ `<MediaFiles>`
- ✅ `<MediaFile>` (минимум один, присутствует семь)

### Атрибуты MediaFile
- ✅ `delivery="progressive"` - корректно
- ✅ `type="video/mp4"` и `type="video/webm"` - корректно
- ✅ `width` и `height` - присутствуют и числовые
- ✅ `bitrate` - присутствует и числовой
- ✅ `isScalable="true"` - корректно (строка)
- ✅ `keepAspectRatio="true"` - корректно (строка)

### События Tracking
- ✅ Все стандартные события VAST 3.0 присутствуют:
  - `creativeView`
  - `start`
  - `firstQuartile`
  - `midpoint`
  - `thirdQuartile`
  - `complete`

### Использование CDATA
- ✅ Все URL обернуты в CDATA секции - отличное решение для защиты от проблем с экранированием XML символов

### Extensions
- ✅ Элемент `<Extensions>` присутствует
- ✅ Кастомные расширения корректно структурированы

---

## 🔍 Проверка соответствия XSD схеме (http://specs.adfox.ru/uploads/vast.xsd)

### Примечание о XSD схеме

XSD схема по адресу `http://specs.adfox.ru/uploads/vast.xsd` является кастомной схемой AdFox и может содержать дополнительные требования, специфичные для их платформы.

**Общие требования XSD валидации:**

1. **XML структура:**
   - ✅ XML декларация присутствует: `<?xml version='1.0' encoding='utf-8'?>`
   - ✅ Корневой элемент `<VAST>` корректен
   - ✅ Namespace объявлен правильно

2. **Типы данных:**
   - ✅ Атрибуты `width`, `height`, `bitrate` - числовые значения
   - ✅ Атрибуты `isScalable`, `keepAspectRatio` - строки "true"/"false"
   - ✅ Формат `Duration` соответствует паттерну времени

3. **Обязательные элементы:**
   - ⚠️ `<AdSystem version="">` - отсутствует обязательный атрибут `version` (будет ошибка XSD валидации)

4. **Минимальные требования:**
   - ✅ Минимум один `<Impression>` - присутствует
   - ✅ Минимум один `<MediaFile>` - присутствует
   - ✅ `<Duration>` присутствует

### Потенциальные проблемы XSD валидации

1. **Отсутствие `version` в `<AdSystem>`**
   - XSD схема AdFox, вероятно, требует этот атрибут
   - Это вызовет ошибку валидации

2. **Формат данных:**
   - Если XSD строго определяет формат `Duration` как `HH:MM:SS.mmm`, то `00:00:15` может вызвать предупреждение
   - Но обычно XSD допускает оба формата

3. **Кастомные Extensions:**
   - XSD может не знать о кастомных типах Extension
   - Это может вызвать предупреждения, но не ошибки (если Extension определен как `xs:anyType`)

---

## 📋 Детальная проверка элементов

### Элемент `<AdSystem>`

**Текущее состояние:**
```xml
<AdSystem>Rollover</AdSystem>
```

**Проблемы:**
- ❌ Отсутствует обязательный атрибут `version`

**Требования VAST 3.0:**
- Атрибут `version` - **ОБЯЗАТЕЛЬНЫЙ**
- Текстовое содержимое - обязательно

**Исправление:**
```xml
<AdSystem version="1.0">Rollover</AdSystem>
```

---

### Элемент `<Duration>`

**Текущее состояние:**
```xml
<Duration>00:00:15</Duration>
```

**Проверка:**
- ✅ Формат `HH:MM:SS` корректен для VAST 3.0
- ✅ Значение валидно (15 секунд)
- 🟡 Рекомендуется формат с миллисекундами: `00:00:15.000`

---

### Элемент `<Linear>` с атрибутом `skipoffset`

**Текущее состояние:**
```xml
<Linear skipoffset="00:00:15">
```

**Проверка:**
- ✅ Формат `HH:MM:SS` корректен
- ⚠️ Логическая проблема: если длительность = skipoffset, пропуск недоступен
- 🟡 Рекомендуется использовать процент или уменьшить значение

---

### Элементы `<MediaFile>`

**Проверка всех 7 MediaFile:**

1. ✅ Все имеют обязательные атрибуты: `delivery`, `type`, `width`, `height`
2. ✅ Все имеют опциональные атрибуты: `bitrate`, `isScalable`, `keepAspectRatio`
3. ✅ Все имеют валидные URL в CDATA
4. ✅ Типы корректны: `video/mp4`, `video/webm`
5. ✅ Delivery корректный: `progressive`
6. ✅ Числовые атрибуты валидны
7. ✅ Булевы атрибуты в правильном формате ("true"/"false")

---

### Элементы `<Tracking>`

**Проверка событий:**

| Событие | Количество | Статус |
|---------|-----------|--------|
| `creativeView` | 1 | ✅ |
| `start` | 3 | ✅ (множественные URL допустимы) |
| `firstQuartile` | 3 | ✅ |
| `midpoint` | 3 | ✅ |
| `thirdQuartile` | 3 | ✅ |
| `complete` | 3 | ✅ |

**Все события:**
- ✅ Имеют атрибут `event`
- ✅ Имеют валидные URL в CDATA
- ✅ События соответствуют спецификации VAST 3.0

---

### Элемент `<Extensions>`

**Текущее состояние:**
```xml
<Extensions>
    <Extension type="startTime">00:00</Extension>
    <Extension type="skipTime">00:15</Extension>
    <Extension type="skipTime2">00:15</Extension>
    <Extension type="linkTxt">перейти на сайт рекламодателя</Extension>
    <Extension type="isClickable">0</Extension>
    <Extension type="AdTitle">...</Extension>
    <Extension type="AdTune">...</Extension>
</Extensions>
```

**Проверка:**
- ✅ Структура корректна
- ✅ Все Extension имеют атрибут `type`
- 🟡 Дублирование: `skipTime` и `skipTime2` с одинаковыми значениями
- ✅ JSON в CDATA допустим для кастомных Extension

---

## 🎯 ИТОГОВЫЙ СПИСОК ПРОБЛЕМ

### 🔴 КРИТИЧЕСКИЕ ОШИБКИ (требуют обязательного исправления):

1. **Отсутствует обязательный атрибут `version` у `<AdSystem>`**
   - Текущее: `<AdSystem>Rollover</AdSystem>`
   - Должно быть: `<AdSystem version="1.0">Rollover</AdSystem>`
   - **Это вызовет ошибку XSD валидации**

### 🟡 РЕКОМЕНДАЦИИ (желательно исправить):

2. **Добавить элемент `<VideoClicks>`** (если нужна интерактивность)
   ```xml
   <VideoClicks>
       <ClickThrough><![CDATA[https://example.com]]></ClickThrough>
   </VideoClicks>
   ```

3. **Улучшить формат `Duration`**
   - Текущее: `00:00:15`
   - Рекомендуется: `00:00:15.000`

4. **Улучшить формат `skipoffset`**
   - Текущее: `00:00:15`
   - Рекомендуется: `00:00:15.000` или `25%`

5. **Исправить логику `skipoffset`**
   - Если длительность = skipoffset, пропуск недоступен
   - Рассмотреть использование процента

6. **Удалить дублирующий Extension**
   - `skipTime` и `skipTime2` с одинаковыми значениями

---

## ✅ ПОЛОЖИТЕЛЬНЫЕ МОМЕНТЫ

1. ✅ Версия VAST указана правильно: `version="3.0"`
2. ✅ Правильная структура документа
3. ✅ Все основные обязательные элементы присутствуют
4. ✅ Использование CDATA для всех URL - отличное решение
5. ✅ Корректные форматы данных
6. ✅ Множественные MediaFile для разных битрейтов и форматов
7. ✅ Корректные события Tracking
8. ✅ Правильные атрибуты MediaFile
9. ✅ Множественные Impression для разных систем аналитики

---

## 📝 ВЫВОД

### Соответствие VAST 3.0:
Документ **почти полностью соответствует** спецификации VAST 3.0, но содержит **одну критическую ошибку**: отсутствие обязательного атрибута `version` у элемента `<AdSystem>`.

### Соответствие XSD схеме:
Документ **не пройдет XSD валидацию** из-за отсутствия обязательного атрибута `version` в `<AdSystem>`. После добавления этого атрибута документ должен пройти валидацию.

### Рекомендуемые действия:
1. **Обязательно:** Добавить `version="1.0"` (или соответствующую версию) к `<AdSystem>`
2. **Желательно:** Добавить `<VideoClicks>` для интерактивности
3. **Желательно:** Улучшить форматы `Duration` и `skipoffset`
4. **Желательно:** Исправить логику `skipoffset`

После исправления критической ошибки документ будет полностью соответствовать спецификации VAST 3.0 и пройдет XSD валидацию.

---

## 🔧 РЕКОМЕНДУЕМЫЕ ИСПРАВЛЕНИЯ

### Исправление 1: AdSystem с version (ОБЯЗАТЕЛЬНО)
```xml
<!-- Было -->
<AdSystem>Rollover</AdSystem>

<!-- Должно быть -->
<AdSystem version="1.0">Rollover</AdSystem>
```

### Исправление 2: Добавить VideoClicks (рекомендуется)
```xml
<VideoClicks>
    <ClickThrough><![CDATA[https://example.com]]></ClickThrough>
</VideoClicks>
```

### Исправление 3: Улучшить Duration (рекомендуется)
```xml
<!-- Было -->
<Duration>00:00:15</Duration>

<!-- Рекомендуется -->
<Duration>00:00:15.000</Duration>
```

### Исправление 4: Улучшить skipoffset (рекомендуется)
```xml
<!-- Вариант 1: С миллисекундами -->
<Linear skipoffset="00:00:15.000">

<!-- Вариант 2: В процентах (если нужно пропустить после 25%) -->
<Linear skipoffset="25%">
```
