# AI Prompt Rules для Docker MCP Orchestrator

## 🎯 Общие принципы

Docker MCP Orchestrator - это **инструмент для управления MCP серверами**. Ты (AI) сам принимаешь решения о том, какие серверы нужны для задачи. Orchestrator предоставляет инструменты для управления и **proxy** для взаимодействия с запущенными серверами.

**ВАЖНО**: Все взаимодействие с запущенными MCP серверами идет **только через Orchestrator**. Не пытайся вызывать tools напрямую - они доступны только через proxy.

---

## 📋 Workflow работы с Orchestrator

### Шаг 1: Изучение доступных серверов

**Всегда начинай с получения списка установленных серверов**:

```
Используй tool: list_installed_servers()
```

Это вернет список всех MCP серверов, которые установлены в Docker MCP Catalog.

**Если нужен новый сервер** (редкий случай):

```
Используй tool: list_catalog_servers()
```

Это покажет все доступные серверы в каталоге для установки.

---

### Шаг 2: Изучение возможностей серверов

**Получи информацию о tools сервера** (без запуска):

```
Используй tool: get_server_tools(server_name="github-mcp")
```

Это вернет метаданные о всех tools, которые предоставляет сервер.

**Получи детальную информацию о сервере**:

```
Используй tool: get_server_info(server_name="github-mcp")
```

Это вернет описание, требования, конфигурацию и другую информацию.

---

### Шаг 3: Настройка конфигурации (если необходимо)

**Многие серверы требуют конфигурации перед запуском**:

#### Установка секретов:

```
Используй tool: secret_set(key="GITHUB_TOKEN", value="your-token")
```

#### Установка конфигурации:

```
Используй tool: config_set(server="postgres-mcp", config={"DATABASE_URL": "postgres://..."})
```

**Важно**: Настрой конфигурацию **перед** запуском сервера.

---

### Шаг 4: Запуск нужных серверов

**Запусти серверы, которые нужны для задачи**:

```
Используй tool: start_servers(servers=["github-mcp", "postgres-mcp"])
```

**Что происходит при запуске**:
1. Orchestrator активирует серверы через Docker MCP Toolkit
2. Orchestrator устанавливает соединения с серверами
3. Orchestrator регистрирует tools в Proxy
4. Orchestrator извлекает промпты серверов (если есть)

**Что ты получишь в ответе**:
- Список активных tools от запущенных серверов
- **Промпты для добавления в контекст** (если есть)
- Статус запуска каждого сервера

**КРИТИЧЕСКИ ВАЖНО**: 
- **Обязательно добавь промпты в контекст**, если они предоставлены
- Промпты содержат важную информацию для правильной работы с сервером
- Без промптов сервер может работать некорректно

---

### Шаг 5: Получение списка активных tools

**После запуска серверов получи список доступных tools**:

```
Используй tool: list_active_tools()
```

Это покажет все tools, которые доступны от активных серверов.

---

### Шаг 6: Использование tools через Proxy

**ВСЕ вызовы tools должны идти через Orchestrator**:

```
Используй tool: call_tool(tool_name="github_create_repo", arguments={"name": "my-repo", "description": "..."})
```

**НЕ пытайся вызывать tools напрямую!** Они доступны только через Orchestrator proxy.

**Формат вызова**:
- `tool_name`: имя tool (например, "github_create_repo")
- `arguments`: словарь с аргументами для tool

**Обработка результатов**:
- Если вызов успешен, получишь результат от сервера
- Если произошла ошибка, получишь детальное описание ошибки

---

### Шаг 7: Остановка серверов после завершения задачи

**После завершения работы останови серверы**:

```
Используй tool: stop_servers(servers=["github-mcp", "postgres-mcp"])
```

**Почему это важно**:
- Освобождает ресурсы
- Минимизирует контекст для следующих задач
- Закрывает соединения с серверами

---

## 🎯 Примеры использования

### Пример 1: Работа с GitHub

```
1. list_installed_servers() → находим "github-mcp"
2. get_server_tools("github-mcp") → изучаем доступные tools
3. secret_set("GITHUB_TOKEN", "token") → настраиваем токен
4. start_servers(["github-mcp"]) → запускаем сервер
   → Получаем промпт, добавляем в контекст
5. list_active_tools() → получаем список tools
6. call_tool("github_create_repo", {"name": "my-repo"}) → создаем репозиторий
7. stop_servers(["github-mcp"]) → останавливаем сервер
```

### Пример 2: Работа с базой данных

```
1. list_installed_servers() → находим "postgres-mcp"
2. get_server_info("postgres-mcp") → изучаем требования
3. config_set("postgres-mcp", {"DATABASE_URL": "postgres://..."}) → настраиваем подключение
4. start_servers(["postgres-mcp"]) → запускаем сервер
   → Получаем промпт, добавляем в контекст
5. call_tool("postgres_query", {"query": "SELECT * FROM users"}) → выполняем запрос
6. stop_servers(["postgres-mcp"]) → останавливаем сервер
```

### Пример 3: Комбинированная работа

```
1. list_installed_servers() → находим нужные серверы
2. Настраиваем конфигурацию для всех серверов
3. start_servers(["github-mcp", "postgres-mcp", "filesystem-mcp"]) → запускаем все сразу
   → Получаем промпты, добавляем все в контекст
4. Используем tools от разных серверов через call_tool()
5. stop_servers([...]) → останавливаем все после завершения
```

---

## ⚠️ Важные правила

### 1. Всегда используй Proxy

**ПРАВИЛЬНО**:
```
call_tool("github_create_repo", {...})
```

**НЕПРАВИЛЬНО**:
```
# Не пытайся вызывать tool напрямую!
github_create_repo(...)  # ❌ Это не сработает
```

### 2. Всегда добавляй промпты в контекст

**ПРАВИЛЬНО**:
```
start_servers(["github-mcp"])
→ Получаем промпт: "# GitHub MCP Server\n\nUse this server..."
→ Добавляем промпт в контекст
```

**НЕПРАВИЛЬНО**:
```
start_servers(["github-mcp"])
→ Игнорируем промпт  # ❌ Сервер может работать некорректно
```

### 3. Настраивай конфигурацию перед запуском

**ПРАВИЛЬНО**:
```
secret_set("GITHUB_TOKEN", "token")
start_servers(["github-mcp"])
```

**НЕПРАВИЛЬНО**:
```
start_servers(["github-mcp"])
secret_set("GITHUB_TOKEN", "token")  # ❌ Слишком поздно
```

### 4. Останавливай серверы после использования

**ПРАВИЛЬНО**:
```
# Работа с сервером
call_tool("github_create_repo", {...})
# Завершение задачи
stop_servers(["github-mcp"])
```

**НЕПРАВИЛЬНО**:
```
# Работа с сервером
call_tool("github_create_repo", {...})
# Забыли остановить  # ❌ Ресурсы не освобождены
```

---

## 🔍 Оптимизация работы

### Минимизация количества активных серверов

**ПРАВИЛЬНО**:
- Запускай только те серверы, которые нужны для текущей задачи
- Останавливай серверы сразу после использования

**НЕПРАВИЛЬНО**:
- Запускать все серверы "на всякий случай"
- Держать серверы активными между задачами

### Последовательный запуск

**Если нужно несколько серверов**:
- Можно запустить все сразу: `start_servers(["server1", "server2", "server3"])`
- Или последовательно, если есть зависимости

### Использование метаданных

**Перед запуском сервера**:
- Всегда изучай `get_server_tools()` для понимания возможностей
- Проверяй `get_server_info()` для требований и конфигурации

---

## 🛠️ Управление конфигурацией

### Секреты

**Установка секрета**:
```
secret_set(key="API_KEY", value="secret-value")
```

**Список секретов**:
```
secret_list()
```

**Удаление секрета**:
```
secret_remove(key="API_KEY")
```

### Конфигурация серверов

**Установка конфигурации**:
```
config_set(server="postgres-mcp", config={
    "DATABASE_URL": "postgres://...",
    "MAX_CONNECTIONS": "10"
})
```

**Получение конфигурации**:
```
config_get(server="postgres-mcp")
```

---

## 📊 Мониторинг и отладка

### Проверка активных серверов

```
get_active_servers()
```

Это покажет список всех запущенных серверов и их статус.

### Проверка доступных tools

```
list_active_tools()
```

Это покажет все tools от активных серверов.

### Обработка ошибок

**Если вызов tool завершился ошибкой**:
1. Проверь, что сервер запущен: `get_active_servers()`
2. Проверь, что tool доступен: `list_active_tools()`
3. Проверь конфигурацию: `config_get(server)`
4. Попробуй перезапустить сервер: `stop_servers()` → `start_servers()`

---

## 🎓 Лучшие практики

1. **Всегда начинай с изучения**: Сначала изучи доступные серверы и их возможности
2. **Настраивай перед запуском**: Конфигурация должна быть готова до запуска сервера
3. **Используй промпты**: Всегда добавляй промпты в контекст при запуске сервера
4. **Используй Proxy**: Все вызовы tools только через `call_tool()`
5. **Останавливай после использования**: Освобождай ресурсы после завершения задачи
6. **Минимизируй активные серверы**: Запускай только то, что нужно сейчас

---

## 📝 Чеклист для работы с MCP серверами

- [ ] Получил список установленных серверов
- [ ] Изучил tools и требования серверов
- [ ] Настроил конфигурацию и секреты
- [ ] Запустил нужные серверы
- [ ] Добавил промпты в контекст
- [ ] Получил список активных tools
- [ ] Использую tools через `call_tool()`
- [ ] Остановил серверы после завершения

---

**Помни: Orchestrator - это инструмент. Ты принимаешь решения, Orchestrator выполняет команды!** 🚀
