# Быстрый старт: Интеграция Cursor и Claude Code

## 🚀 Краткое описание

Этот проект реализует интеграцию между **Cursor IDE** и **Claude Code** для создания синергии:
- **Cursor** улучшает и форматирует запросы пользователей
- **Claude Code** оркестрирует выполнение через субагентов
- Взаимодействие происходит через **MCP Bridge Server**

---

## ⚡ Быстрая установка (5 минут)

### Шаг 1: Установка зависимостей

```bash
# Клонирование репозитория Bridge Server (когда будет готов)
cd cursor-claude-code-bridge
pip install -r requirements.txt
```

### Шаг 2: Настройка API ключа

```bash
export ANTHROPIC_API_KEY=your-api-key-here
```

Или создайте файл `.env`:
```bash
ANTHROPIC_API_KEY=your-api-key-here
```

### Шаг 3: Настройка Cursor

Добавьте в `.cursor/mcp.json` или `~/.config/cursor/mcp.json`:

```json
{
  "mcpServers": {
    "cursor-claude-code-bridge": {
      "command": "python",
      "args": ["-m", "bridge"],
      "cwd": "/absolute/path/to/cursor-claude-code-bridge",
      "env": {
        "ANTHROPIC_API_KEY": "your-api-key-here"
      }
    }
  }
}
```

### Шаг 4: Перезапуск Cursor

Перезапустите Cursor IDE для применения изменений.

### Шаг 5: Проверка

В Cursor попробуйте:
```
"Создай функцию для валидации email адресов"
```

Cursor должен автоматически:
1. Уточнить требования
2. Улучшить запрос
3. Отправить в Claude Code через Bridge
4. Показать результаты выполнения

---

## 📚 Документация

- **[Полный план интеграции](./CURSOR_CLAUDE_CODE_INTEGRATION_PLAN.md)** - детальный план реализации
- **[Структура Bridge Server](./implementation_examples/bridge_server_structure.md)** - примеры кода
- **[Конфигурация Cursor](./implementation_examples/cursor_configuration.md)** - настройка Cursor
- **[Промпт для Claude Code](./implementation_examples/prompts/claude_code_orchestrator.md)** - промпт оркестратора

---

## 🏗️ Архитектура (кратко)

```
Пользователь → Cursor (улучшение запроса) 
            → Bridge MCP Server 
            → Claude Code (оркестрация) 
            → Docker MCP Orchestrator 
            → Sub-Agents (выполнение)
            → Результаты → Cursor → Пользователь
```

---

## 🔧 Основные компоненты

1. **Cursor Request Enhancement** - улучшение запросов (встроено в Cursor)
2. **Bridge MCP Server** - мост между Cursor и Claude Code
3. **Claude Code Orchestrator** - оркестрация через Anthropic API
4. **Docker MCP Orchestrator** - управление субагентами (уже существует)

---

## 📋 Чеклист установки

- [ ] Установлены зависимости Bridge Server
- [ ] Настроен ANTHROPIC_API_KEY
- [ ] Добавлен Bridge в конфигурацию Cursor
- [ ] Перезапущен Cursor
- [ ] Протестирован простой запрос

---

## 🆘 Troubleshooting

### Bridge Server не запускается
- Проверьте путь в `mcp.json`
- Проверьте установку зависимостей: `pip install -r requirements.txt`
- Проверьте логи: `logs/bridge.log`

### Claude Code не отвечает
- Проверьте API ключ: `echo $ANTHROPIC_API_KEY`
- Проверьте баланс API ключа
- Проверьте интернет соединение

### Cursor не видит Bridge tools
- Убедитесь, что конфигурация MCP правильная
- Перезапустите Cursor
- Проверьте логи Cursor

---

## 📞 Поддержка

Для вопросов и проблем:
1. Проверьте документацию
2. Проверьте логи
3. Создайте issue в репозитории

---

**Время установки**: ~5 минут  
**Сложность**: Средняя  
**Требования**: Python 3.11+, Cursor IDE, Anthropic API Key
