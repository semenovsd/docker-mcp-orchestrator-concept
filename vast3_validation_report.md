# Отчет о проверке VAST документа на соответствие спецификации VAST 3.0

## 📋 Итоговая оценка

**Статус:** ❌ **ДОКУМЕНТ НЕ СООТВЕТСТВУЕТ СПЕЦИФИКАЦИИ VAST 3.0**

**Обнаружено ошибок:** 16 критических ошибок

---

## 🔴 КРИТИЧЕСКИЕ ОШИБКИ

### 1. Неверная версия VAST

**Проблема:**
```xml
<VAST ... version="2.0" ...>
```

**Ошибка:** В документе указана версия `2.0`, но для соответствия спецификации VAST 3.0 требуется строго `version="3.0"`.

**Спецификация VAST 3.0:** Атрибут `version` должен быть равен `"3.0"`.

**Исправление:**
```xml
<VAST xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" version="3.0" xsi:noNamespaceSchemaLocation="...">
```

**Критичность:** 🔴 **КРИТИЧНО** - Без этого документа не может считаться VAST 3.0 документом.

---

### 2. Пустой элемент `<ClickThrough>`

**Проблема:**
```xml
<VideoClicks>
    <ClickThrough></ClickThrough>
</VideoClicks>
```

**Ошибка:** Элемент `<ClickThrough>` пустой. В VAST 3.0 элемент должен либо содержать валидный URL, либо быть полностью удален.

**Спецификация VAST 3.0:** 
- Если URL для клика есть → элемент должен содержать URL
- Если URL для клика нет → элемент должен быть удален (или весь `<VideoClicks>`)

**Исправление:**

**Вариант 1:** Если URL есть:
```xml
<VideoClicks>
    <ClickThrough><![CDATA[https://example.com/click]]></ClickThrough>
</VideoClicks>
```

**Вариант 2:** Если URL нет (рекомендуется):
```xml
<!-- Элемент VideoClicks полностью удален -->
```

**Критичность:** 🔴 **КРИТИЧНО** - Пустые обязательные элементы недопустимы в VAST 3.0.

---

### 3. Неверные атрибуты в `<MediaFile>` (14 ошибок в 7 элементах)

**Проблема:**
Во всех 7 элементах `<MediaFile>` используются неверные атрибуты:
- `isScalable="true"` ❌
- `keepAspectRatio="true"` ❌

**Ошибка:** В VAST 3.0 правильные атрибуты:
- `scalable="true"` ✅ (boolean, не `isScalable`)
- `maintainAspectRatio="true"` ✅ (boolean, не `keepAspectRatio`)

**Спецификация VAST 3.0:** 
Согласно разделу 2.3.3.2 MediaFile, допустимые атрибуты:
- `id` (optional)
- `delivery` (required) - "progressive" или "streaming"
- `type` (required) - MIME type
- `bitrate` (optional)
- `width` (optional)
- `height` (optional)
- `scalable` (optional, boolean) - **НЕ `isScalable`**
- `maintainAspectRatio` (optional, boolean) - **НЕ `keepAspectRatio`**
- `codec` (optional)
- `apiFramework` (optional)
- `minBitrate` (optional)
- `maxBitrate` (optional)

**Затронутые MediaFile элементы:**
1. `id="01970c8d-0633-77d7-9176-d895f2439ff9"`
2. `id="01970c8d-0623-7c82-9098-04a227c796d0"`
3. `id="01970c8d-0647-7061-ba3d-be89be0476e7"`
4. `id="01970c8d-0628-73b9-9776-a3458b88b243"`
5. `id="01970c8d-0637-7b12-9a40-1ae6f147e85d"`
6. `id="01970c8d-0643-729e-ad91-fda091c20f37"`
7. `id="01970c8d-0653-76de-9433-71b237fb80dc"`

**Исправление:**

**БЫЛО:**
```xml
<MediaFile id="..." delivery="progressive" type="video/mp4" bitrate="1200" width="854" height="480" isScalable="true" keepAspectRatio="true">
```

**ДОЛЖНО БЫТЬ:**
```xml
<MediaFile id="..." delivery="progressive" type="video/mp4" bitrate="1200" width="854" height="480" scalable="true" maintainAspectRatio="true">
```

**Критичность:** 🔴 **КРИТИЧНО** - Неверные атрибуты нарушают структуру VAST 3.0 и могут привести к отклонению валидаторами.

---

## ✅ Что сделано правильно

1. **CDATA секции** - Правильное использование CDATA для URL решает проблемы с экранированием символов `&`
2. **Namespace xsi** - Правильно объявлен `xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"`
3. **Обязательные элементы** - Присутствуют `AdSystem`, `AdTitle`, `Creatives`, `Duration`, `MediaFiles`
4. **Формат Duration** - "00:00:15" соответствует формату HH:MM:SS
5. **Формат skipoffset** - "00:00:15" соответствует формату HH:MM:SS
6. **Структура элементов** - Основная структура корректна

---

## 📊 Сводная таблица ошибок

| № | Тип | Элемент | Проблема | Критичность | Количество |
|---|-----|---------|----------|-------------|------------|
| 1 | Версия | `<VAST>` | Версия указана как "2.0" вместо "3.0" | 🔴 Критично | 1 |
| 2 | Пустой элемент | `<ClickThrough>` | Пустой элемент недопустим | 🔴 Критично | 1 |
| 3 | Неверный атрибут | `<MediaFile>` | `isScalable` вместо `scalable` | 🔴 Критично | 7 |
| 4 | Неверный атрибут | `<MediaFile>` | `keepAspectRatio` вместо `maintainAspectRatio` | 🔴 Критично | 7 |

**Всего критических ошибок:** 16

---

## 🔧 Инструкция по исправлению

### Шаг 1: Изменить версию VAST
```xml
<!-- БЫЛО -->
<VAST ... version="2.0" ...>

<!-- ДОЛЖНО БЫТЬ -->
<VAST ... version="3.0" ...>
```

### Шаг 2: Удалить или заполнить ClickThrough
```xml
<!-- УДАЛИТЬ пустой элемент -->
<!-- БЫЛО -->
<VideoClicks>
    <ClickThrough></ClickThrough>
</VideoClicks>

<!-- ДОЛЖНО БЫТЬ (если URL нет) -->
<!-- Элемент удален -->
```

### Шаг 3: Исправить атрибуты MediaFile
Заменить во всех 7 элементах `<MediaFile>`:

```xml
<!-- БЫЛО -->
isScalable="true" keepAspectRatio="true"

<!-- ДОЛЖНО БЫТЬ -->
scalable="true" maintainAspectRatio="true"
```

Можно использовать поиск и замену:
- `isScalable="true"` → `scalable="true"`
- `keepAspectRatio="true"` → `maintainAspectRatio="true"`

---

## 📝 Дополнительные замечания

### Допустимые элементы (не ошибки)

1. **Extensions** - Нестандартные Extension элементы (`startTime`, `skipTime`, `linkTxt`, и т.д.) допустимы в VAST 3.0, так как Extension предназначены для расширений.

2. **Дублирование Tracking событий** - Множественные Tracking элементы с одинаковым `event` допустимы в VAST 3.0 (например, несколько `start` событий от разных трекинговых систем).

3. **CDATA в URL** - Использование CDATA для URL является правильным подходом и решает проблемы с экранированием символов.

---

## ✅ После исправления

После внесения всех исправлений документ будет полностью соответствовать спецификации **VAST 3.0**.

**Проверка:** Используйте валидатор `vast3_validator.py` для автоматической проверки:
```bash
python3 vast3_validator.py your_vast_file.xml
```

---

## 📚 Ссылки на спецификацию

- **VAST 3.0 Specification:** http://www.iab.com/guidelines/digital-video-ad-serving-template-vast-3-0/
- **MediaFile Attributes:** Раздел 2.3.3.2 спецификации VAST 3.0
- **ClickThrough Element:** Раздел 2.3.3.1.2 спецификации VAST 3.0
